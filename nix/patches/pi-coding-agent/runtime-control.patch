diff --git a/packages/coding-agent/src/core/agent-session-runtime.ts b/packages/coding-agent/src/core/agent-session-runtime.ts
index 861c00f7..d9868850 100644
--- a/packages/coding-agent/src/core/agent-session-runtime.ts
+++ b/packages/coding-agent/src/core/agent-session-runtime.ts
@@ -79,6 +79,17 @@ export class AgentSessionRuntime {
 	private readonly createRuntime: CreateAgentSessionRuntimeFactory;
 	private _diagnostics: AgentSessionRuntimeDiagnostic[];
 	private _modelFallbackMessage?: string;
+	private runtimeControlReplacementDepth = 0;
+
+	private async withRuntimeControlReplacement<T>(run: () => Promise<T>): Promise<T> {
+		this.runtimeControlReplacementDepth++;
+		try { return await run(); } finally { this.runtimeControlReplacementDepth--; }
+	}
+
+	switchSession(...args: Parameters<AgentSessionRuntime["_switchSession"]>) { return this.withRuntimeControlReplacement(() => this._switchSession(...args)); }
+	newSession(...args: Parameters<AgentSessionRuntime["_newSession"]>) { return this.withRuntimeControlReplacement(() => this._newSession(...args)); }
+	fork(...args: Parameters<AgentSessionRuntime["_fork"]>) { return this.withRuntimeControlReplacement(() => this._fork(...args)); }
+	importFromJsonl(...args: Parameters<AgentSessionRuntime["_importFromJsonl"]>) { return this.withRuntimeControlReplacement(() => this._importFromJsonl(...args)); }
 
 	constructor(
 		_session: AgentSession,
@@ -88,6 +99,7 @@ export class AgentSessionRuntime {
 		_modelFallbackMessage?: string,
 	) {
 		this._session = _session;
+		_session.extensionRunner.bindRuntimeControlOwnerAdmission(() => this.runtimeControlReplacementDepth === 0);
 		this._services = _services;
 		this.createRuntime = createRuntime;
 		this._diagnostics = _diagnostics;
@@ -179,6 +191,7 @@ export class AgentSessionRuntime {
 
 	private apply(result: CreateAgentSessionRuntimeResult): void {
 		this._session = result.session;
+		result.session.extensionRunner.bindRuntimeControlOwnerAdmission(() => this.runtimeControlReplacementDepth === 0);
 		this._services = result.services;
 		this._diagnostics = result.diagnostics;
 		this._modelFallbackMessage = result.modelFallbackMessage;
@@ -193,7 +206,7 @@ export class AgentSessionRuntime {
 		}
 	}
 
-	async switchSession(
+	private async _switchSession(
 		sessionPath: string,
 		options?: {
 			cwdOverride?: string;
@@ -223,7 +236,7 @@ export class AgentSessionRuntime {
 		return { cancelled: false };
 	}
 
-	async newSession(options?: {
+	private async _newSession(options?: {
 		parentSession?: string;
 		setup?: (sessionManager: SessionManager) => Promise<void>;
 		withSession?: (ctx: ReplacedSessionContext) => Promise<void>;
@@ -259,7 +272,7 @@ export class AgentSessionRuntime {
 		return { cancelled: false };
 	}
 
-	async fork(
+	private async _fork(
 		entryId: string,
 		options?: { position?: "before" | "at"; withSession?: (ctx: ReplacedSessionContext) => Promise<void> },
 	): Promise<{ cancelled: boolean; selectedText?: string }> {
@@ -358,7 +371,7 @@ export class AgentSessionRuntime {
 	 * @throws {SessionImportFileNotFoundError} When the input path does not exist.
 	 * @throws {MissingSessionCwdError} When the imported session cwd cannot be resolved and no override is provided.
 	 */
-	async importFromJsonl(inputPath: string, cwdOverride?: string): Promise<{ cancelled: boolean }> {
+	private async _importFromJsonl(inputPath: string, cwdOverride?: string): Promise<{ cancelled: boolean }> {
 		const resolvedPath = resolvePath(inputPath);
 		if (!existsSync(resolvedPath)) {
 			throw new SessionImportFileNotFoundError(resolvedPath);
diff --git a/packages/coding-agent/src/core/agent-session.ts b/packages/coding-agent/src/core/agent-session.ts
index 8799d17e..b2270582 100644
--- a/packages/coding-agent/src/core/agent-session.ts
+++ b/packages/coding-agent/src/core/agent-session.ts
@@ -1106,10 +1106,20 @@ export class AgentSession {
 	// Prompting
 	// =========================================================================
 
-	private async _runAgentPrompt(messages: AgentMessage | AgentMessage[]): Promise<void> {
+	/** Start an owned background archive's already-admitted user entry once,
+	 * through the ordinary retry/queue/settled pipeline without re-appending it. */
+	async continueAdmittedTurn(): Promise<void> {
+		this._extensionRunner.assertRuntimeControlAdmission();
+		if (this.messages.at(-1)?.role !== "user") throw new Error("No admitted user turn to continue");
+		await this._runAgentPrompt(undefined);
+	}
+
+	private async _runAgentPrompt(messages: AgentMessage | AgentMessage[] | undefined): Promise<void> {
+		if (this.sessionManager.isRuntimeControlQuarantined()) throw new Error("Session writer quarantined; reopen under a new owner");
 		this._isAgentRunActive = true;
 		try {
-			await this.agent.prompt(messages);
+			if (messages === undefined) await this.agent.continue();
+			else await this.agent.prompt(messages);
 			while (await this._handlePostAgentRun()) {
 				await this.agent.continue();
 			}
@@ -1160,7 +1170,16 @@ export class AgentSession {
 	 * @throws Error if streaming and no streamingBehavior specified
 	 * @throws Error if no model selected or no API key available (when not streaming)
 	 */
+	private controlPromptDepth = 0;
+
 	async prompt(text: string, options?: PromptOptions): Promise<void> {
+		if (this.sessionManager.isRuntimeControlQuarantined()) throw new Error("Session writer quarantined; reopen under a new owner");
+		this.controlPromptDepth++;
+		try { return await this._promptForOwner(text, options); }
+		finally { this.controlPromptDepth--; }
+	}
+
+	private async _promptForOwner(text: string, options?: PromptOptions): Promise<void> {
 		const expandPromptTemplates = options?.expandPromptTemplates ?? true;
 		const preflightResult = options?.preflightResult;
 		let messages: AgentMessage[] | undefined;
@@ -2548,8 +2567,21 @@ export class AgentSession {
 		this.agent.state.model = refreshedModel;
 	}
 
+	/** Durable no-run owner transaction; no awaited callbacks inside its fence. */
+	commitRuntimeControl(sessionId: string, leafId: string | null, entries: import("./session-manager.ts").RuntimeControlInput[]): string[] {
+		this._extensionRunner.assertRuntimeControlAdmission();
+		const ids = this.sessionManager.commitRuntimeControl(sessionId, leafId, entries);
+		this.agent.state.messages = this.sessionManager.buildSessionContext().messages;
+		for (const id of ids) {
+			const entry = this.sessionManager.getEntry(id);
+			if (entry) this._emit({ type: "entry_appended", entry });
+		}
+		return ids;
+	}
+
 	private _bindExtensionCore(runner: ExtensionRunner): void {
 		runner.bindCommandAdmission(() => this.isIdle && this._agentSettledDispatchDepth === 0);
+		runner.bindRuntimeControlPromptAdmission(() => !this.sessionManager.isRuntimeControlQuarantined() && this.controlPromptDepth === 0 && this.pendingMessageCount === 0 && this._pendingNextTurnMessages.length === 0 && this._pendingBashMessages.length === 0 && !this.isCompacting && !this.isBashRunning);
 		const getCommands = (): SlashCommandInfo[] => {
 			const extensionCommands: SlashCommandInfo[] = runner.getRegisteredCommands().map((command) => ({
 				name: command.invocationName,
@@ -2595,6 +2627,7 @@ export class AgentSession {
 						});
 					});
 				},
+				commitRuntimeControl: (sessionId, leafId, entries) => this.commitRuntimeControl(sessionId, leafId, entries),
 				appendEntry: (customType, data) => {
 					const entryId = this.sessionManager.appendCustomEntry(customType, data);
 					const entry = this.sessionManager.getEntry(entryId);
diff --git a/packages/coding-agent/src/core/extensions/loader.ts b/packages/coding-agent/src/core/extensions/loader.ts
index ca87f135..6082505f 100644
--- a/packages/coding-agent/src/core/extensions/loader.ts
+++ b/packages/coding-agent/src/core/extensions/loader.ts
@@ -200,6 +200,8 @@ export function createExtensionRuntime(): ExtensionRuntime {
 		refreshTools: () => {},
 		getCommands: notInitialized,
 		invokeExtensionCommand: async () => notInitialized(),
+		commitRuntimeControl: notInitialized,
+		isRuntimeControlAvailable: () => false,
 		setModel: () => Promise.reject(new Error("Extension runtime not initialized")),
 		getThinkingLevel: notInitialized,
 		setThinkingLevel: notInitialized,
@@ -408,6 +410,13 @@ function createExtensionAPI(
 			runtime.setActiveTools(toolNames);
 		},
 
+		isRuntimeControlAvailable() { return runtime.isRuntimeControlAvailable(); },
+
+		commitRuntimeControl(sessionId, leafId, entries) {
+			runtime.assertActive();
+			return runtime.commitRuntimeControl(sessionId, leafId, entries);
+		},
+
 		async invokeExtensionCommand(name, args) {
 			runtime.assertActive();
 			await runtime.invokeExtensionCommand(name, args);
diff --git a/packages/coding-agent/src/core/extensions/runner.ts b/packages/coding-agent/src/core/extensions/runner.ts
index 84625928..9a8a847d 100644
--- a/packages/coding-agent/src/core/extensions/runner.ts
+++ b/packages/coding-agent/src/core/extensions/runner.ts
@@ -340,6 +340,14 @@ export class ExtensionRunner {
 		this.runtime.refreshTools = actions.refreshTools;
 		this.runtime.getCommands = actions.getCommands;
 		this.runtime.invokeExtensionCommand = (name, args) => this.invokeExtensionCommand(name, args);
+		this.runtime.isRuntimeControlAvailable = () => {
+			try { this.assertRuntimeControlAdmission(); return true; } catch { return false; }
+		};
+		this.runtime.commitRuntimeControl = (sessionId, leafId, entries) => {
+			this.assertRuntimeControlAdmission();
+			if (!actions.commitRuntimeControl) throw new Error("Runtime control unavailable");
+			return actions.commitRuntimeControl(sessionId, leafId, entries);
+		};
 		this.runtime.setModel = actions.setModel;
 		this.runtime.getThinkingLevel = actions.getThinkingLevel;
 		this.runtime.setThinkingLevel = actions.setThinkingLevel;
@@ -713,6 +721,18 @@ export class ExtensionRunner {
 		this.commandAdmissionFn = admit;
 	}
 
+	private runtimeControlOwnerAdmission = () => true;
+	private runtimeControlPromptAdmission = () => true;
+	bindRuntimeControlOwnerAdmission(admit: () => boolean): void { this.runtimeControlOwnerAdmission = admit; }
+	bindRuntimeControlPromptAdmission(admit: () => boolean): void { this.runtimeControlPromptAdmission = admit; }
+
+	assertRuntimeControlAdmission(): void {
+		this.assertActive();
+		this.runtime.assertActive();
+		if (!this.commandAdmissionFn() || !this.runtimeControlOwnerAdmission() || !this.runtimeControlPromptAdmission() || this.eventDispatchDepth !== 0 || this.commandActive)
+			throw new Error("Runtime control requires idle owner outside command/event dispatch");
+	}
+
 	async invokeExtensionCommand(name: string, args = ""): Promise<void> {
 		this.assertActive();
 		this.runtime.assertActive();
diff --git a/packages/coding-agent/src/core/extensions/types.ts b/packages/coding-agent/src/core/extensions/types.ts
index d8793e30..c2b76183 100644
--- a/packages/coding-agent/src/core/extensions/types.ts
+++ b/packages/coding-agent/src/core/extensions/types.ts
@@ -1424,6 +1424,13 @@ export interface ExtensionAPI {
 	 */
 	invokeExtensionCommand(name: string, args?: string): Promise<void>;
 
+	/** Familiar owner-only atomic no-run control write. Synchronous, idle/session/leaf fenced.
+	 * Rejects awaited event dispatch, active commands and stale runtimes. Never queues inference.
+	 */
+	commitRuntimeControl(sessionId: string, leafId: string | null, entries: import("../session-manager.ts").RuntimeControlInput[]): string[];
+	/** Non-mutating current eligibility hint. commitRuntimeControl rechecks atomically. */
+	isRuntimeControlAvailable(): boolean;
+
 	// =========================================================================
 	// Model and Thinking Level
 	// =========================================================================
@@ -1685,6 +1692,8 @@ export type SetLabelHandler = (entryId: string, label: string | undefined) => vo
 export interface ExtensionRuntimeState {
 	/** Bound by ExtensionRunner, not a caller-supplied raw command handler. */
 	invokeExtensionCommand: ExtensionAPI["invokeExtensionCommand"];
+	isRuntimeControlAvailable: ExtensionAPI["isRuntimeControlAvailable"];
+	commitRuntimeControl: ExtensionAPI["commitRuntimeControl"];
 	flagValues: Map<string, boolean | string>;
 	/** Legacy provider-config registrations queued during extension loading, processed when runner binds. */
 	pendingProviderRegistrations: Array<{ name: string; config: ProviderConfig; extensionPath: string }>;
@@ -1712,6 +1721,7 @@ export interface ExtensionRuntimeState {
  * Provided to runner.initialize(), copied into the shared runtime.
  */
 export interface ExtensionActions {
+	commitRuntimeControl: ExtensionAPI["commitRuntimeControl"];
 	sendMessage: SendMessageHandler;
 	sendUserMessage: SendUserMessageHandler;
 	appendEntry: AppendEntryHandler;
diff --git a/packages/coding-agent/src/core/session-manager.ts b/packages/coding-agent/src/core/session-manager.ts
index d25b196a..58c7c2eb 100644
--- a/packages/coding-agent/src/core/session-manager.ts
+++ b/packages/coding-agent/src/core/session-manager.ts
@@ -6,6 +6,9 @@ import {
 	closeSync,
 	createReadStream,
 	existsSync,
+	fsyncSync,
+	renameSync,
+	unlinkSync,
 	mkdirSync,
 	openSync,
 	readdirSync,
@@ -14,7 +17,7 @@ import {
 	writeFileSync,
 } from "fs";
 import { readdir, stat } from "fs/promises";
-import { join, resolve } from "path";
+import { dirname, join, resolve } from "path";
 import { createInterface } from "readline";
 import { StringDecoder } from "string_decoder";
 import { APP_NAME, getAgentDir as getDefaultAgentDir, getSessionsDir } from "../config.ts";
@@ -29,6 +32,12 @@ import {
 
 export const CURRENT_SESSION_VERSION = 3;
 
+/** Owner-only no-run batch. Cannot manufacture assistant messages. */
+export type RuntimeControlInput =
+	| { type: "message"; message: Extract<Message, { role: "user" }> }
+	| { type: "custom"; customType: string; data: unknown }
+	| { type: "custom_message"; customType: string; content: string; display: boolean; details?: unknown };
+
 export interface SessionHeader {
 	type: "session";
 	version?: number; // v1 sessions don't have this
@@ -990,7 +999,100 @@ export class SessionManager {
 		}
 	}
 
+	private controlWriteUncertain = false;
+	isRuntimeControlQuarantined(): boolean { return this.controlWriteUncertain; }
+	private persistenceBudget = Number.POSITIVE_INFINITY;
+
+	/** Host-owned branch disk bound; checked before publishing an appended entry. */
+	setPersistenceBudget(bytes: number): void {
+		if (!Number.isSafeInteger(bytes) || bytes < 1024 || bytes > 32 * 1024 * 1024) throw new Error("Invalid persistence budget");
+		this.persistenceBudget = bytes;
+	}
+
+	private assertPersistenceBudget(entry?: SessionEntry): void {
+		if (!Number.isFinite(this.persistenceBudget)) return;
+		let bytes = 0;
+		for (const item of this.fileEntries) bytes += Buffer.byteLength(JSON.stringify(item)) + 1;
+		if (entry) bytes += Buffer.byteLength(JSON.stringify(entry)) + 1;
+		if (bytes > this.persistenceBudget) throw new Error("Session persistence byte budget exceeded");
+	}
+
+	/** Caller owns the writer lease and lifecycle fence. Memory changes only after
+	 * atomic rename + directory fsync. Post-rename failure quarantines this writer.
+	 */
+	commitRuntimeControl(expectedSessionId: string, expectedLeafId: string | null, inputs: RuntimeControlInput[], boundary: (point: string) => void = () => {}): string[] {
+		if (this.controlWriteUncertain) throw new Error("Session writer quarantined");
+		if (this.sessionId !== expectedSessionId || this.leafId !== expectedLeafId) throw new Error("Session/leaf conflict");
+		if (!this.persist || !this.sessionFile) throw new Error("Runtime control requires durable session");
+		if (!Array.isArray(inputs) || inputs.length < 1 || inputs.length > 3) throw new Error("Invalid runtime control batch");
+		const encodeWithin = (value: unknown, limit: number): string => {
+			let estimate = 0;
+			const encoded = JSON.stringify(value, (key, item) => {
+				estimate += key.length + (typeof item === "string" ? item.length : 1);
+				if (estimate > limit || (Array.isArray(item) && item.length * 2 > limit)) throw new Error("Runtime control byte budget");
+				return item;
+			});
+			if (encoded === undefined || Buffer.byteLength(encoded) > limit) throw new Error("Runtime control byte budget");
+			return encoded;
+		};
+		const serialized = encodeWithin(inputs, 8 * 1024 * 1024);
+		const copy = JSON.parse(serialized) as RuntimeControlInput[];
+		for (const input of copy) {
+			if (input.type === "message" ? input.message?.role !== "user" :
+				!["custom", "custom_message"].includes(input.type) || typeof input.customType !== "string")
+				throw new Error("Invalid runtime control entry");
+		}
+		for (const type of ["message", "custom", "custom_message"])
+			if (copy.filter((entry) => entry.type === type).length > 1) throw new Error("Invalid runtime control batch");
+		let parentId = this.leafId;
+		const entries: SessionEntry[] = copy.map((input) => {
+			const entry = { ...input, id: randomUUID(), parentId, timestamp: new Date().toISOString() } as SessionEntry;
+			parentId = entry.id;
+			return entry;
+		});
+		const limit = Math.min(this.persistenceBudget, 32 * 1024 * 1024);
+		if (this.flushed && (!existsSync(this.sessionFile) || statSync(this.sessionFile).size > limit)) throw new Error("Canonical archive missing or over byte budget");
+		const next = [...this.fileEntries, ...entries];
+		let bytes = 0;
+		const lines: string[] = [];
+		for (const entry of next) {
+			const line = encodeWithin(entry, limit - bytes - 1) + "\n";
+			bytes += Buffer.byteLength(line);
+			lines.push(line);
+		}
+		const body = lines.join("");
+		// One bounded temporary per canonical file. Only the exclusive owner may
+		// enter this method, so an existing temporary belongs to its dead birth.
+		const temporary = `${this.sessionFile}.runtime-control.tmp`;
+		if (existsSync(temporary)) unlinkSync(temporary);
+		let renamed = false;
+		const fd = openSync(temporary, "wx", 0o600);
+		try {
+			writeFileSync(fd, body);
+			boundary("control:written");
+			fsyncSync(fd);
+			boundary("control:fsynced");
+			renameSync(temporary, this.sessionFile);
+			renamed = true;
+			this.controlWriteUncertain = true;
+			boundary("control:renamed");
+			const directory = openSync(dirname(this.sessionFile), "r");
+			try { fsyncSync(directory); } finally { closeSync(directory); }
+			boundary("control:directory-synced");
+			this.fileEntries = next;
+			this._buildIndex();
+			this.flushed = true;
+			this.controlWriteUncertain = false;
+			return entries.map((entry) => entry.id);
+		} finally {
+			closeSync(fd);
+			if (!renamed) unlinkSync(temporary);
+		}
+	}
+
 	private _rewriteFile(): void {
+		this.assertPersistenceBudget();
+		if (this.controlWriteUncertain) throw new Error("Session writer quarantined");
 		if (!this.persist || !this.sessionFile) return;
 		const fd = openSync(this.sessionFile, "w");
 		try {
@@ -1027,6 +1129,7 @@ export class SessionManager {
 	}
 
 	_persist(entry: SessionEntry): void {
+		if (this.controlWriteUncertain) throw new Error("Session writer quarantined");
 		if (!this.persist || !this.sessionFile) return;
 
 		const hasAssistant = this.fileEntries.some((e) => e.type === "message" && e.message.role === "assistant");
@@ -1056,6 +1159,8 @@ export class SessionManager {
 	}
 
 	private _appendEntry(entry: SessionEntry): void {
+		this.assertPersistenceBudget(entry);
+		if (this.controlWriteUncertain) throw new Error("Session writer quarantined");
 		this.fileEntries.push(entry);
 		this.byId.set(entry.id, entry);
 		this.leafId = entry.id;
