diff --git a/packages/coding-agent/src/core/agent-session-services.ts b/packages/coding-agent/src/core/agent-session-services.ts
index 531f51d..e8f80e8 100644
--- a/packages/coding-agent/src/core/agent-session-services.ts
+++ b/packages/coding-agent/src/core/agent-session-services.ts
@@ -3,7 +3,7 @@ import type { ThinkingLevel } from "@earendil-works/pi-agent-core";
 import type { Model } from "@earendil-works/pi-ai";
 import { getAgentDir } from "../config.ts";
 import { resolvePath } from "../utils/paths.ts";
-import type { SessionStartEvent, ToolDefinition } from "./extensions/index.ts";
+import type { ModelBootstrapRequest, SessionStartEvent, ToolDefinition } from "./extensions/index.ts";
 import { ModelRuntime } from "./model-runtime.ts";
 import {
 	DefaultResourceLoader,
@@ -127,6 +127,53 @@ function applyExtensionFlagValues(
 	return diagnostics;
 }
 
+function flushExtensionProviders(services: AgentSessionServices): void {
+	const extensionsResult = services.resourceLoader.getExtensions();
+	for (const { name, config, extensionPath } of extensionsResult.runtime.pendingProviderRegistrations) {
+		try {
+			services.modelRuntime.registerProvider(name, config);
+		} catch (error) {
+			const message = error instanceof Error ? error.message : String(error);
+			services.diagnostics.push({ type: "error", message: `Extension "${extensionPath}" error: ${message}` });
+		}
+	}
+	extensionsResult.runtime.pendingProviderRegistrations = [];
+	for (const { provider, extensionPath } of extensionsResult.runtime.pendingNativeProviderRegistrations) {
+		try {
+			services.modelRuntime.registerNativeProvider(provider);
+		} catch (error) {
+			const message = error instanceof Error ? error.message : String(error);
+			services.diagnostics.push({ type: "error", message: `Extension "${extensionPath}" error: ${message}` });
+		}
+	}
+	extensionsResult.runtime.pendingNativeProviderRegistrations = [];
+}
+
+/**
+ * Await extension-owned provider materialization for the one model Pi is about
+ * to resolve. This is intentionally not a lifecycle event: session actions and
+ * context do not exist yet, and only queued provider registrations are flushed.
+ */
+export async function bootstrapExtensionModels(
+	services: AgentSessionServices,
+	request: ModelBootstrapRequest,
+): Promise<void> {
+	for (const extension of services.resourceLoader.getExtensions().extensions) {
+		for (const handler of extension.modelBootstraps) {
+			try {
+				await handler(Object.freeze({ ...request }));
+			} catch (error) {
+				const message = error instanceof Error ? error.message : String(error);
+				services.diagnostics.push({
+					type: "error",
+					message: `Extension "${extension.path}" model bootstrap error: ${message}`,
+				});
+			}
+		}
+	}
+	flushExtensionProviders(services);
+}
+
 /**
  * Create cwd-bound runtime services.
  *
@@ -153,43 +200,18 @@ export async function createAgentSessionServices(
 	});
 	await resourceLoader.reload(options.resourceLoaderReloadOptions);
 
-	const diagnostics: AgentSessionRuntimeDiagnostic[] = [];
-	const extensionsResult = resourceLoader.getExtensions();
-	for (const { name, config, extensionPath } of extensionsResult.runtime.pendingProviderRegistrations) {
-		try {
-			modelRuntime.registerProvider(name, config);
-		} catch (error) {
-			const message = error instanceof Error ? error.message : String(error);
-			diagnostics.push({
-				type: "error",
-				message: `Extension "${extensionPath}" error: ${message}`,
-			});
-		}
-	}
-	extensionsResult.runtime.pendingProviderRegistrations = [];
-	for (const { provider, extensionPath } of extensionsResult.runtime.pendingNativeProviderRegistrations) {
-		try {
-			modelRuntime.registerNativeProvider(provider);
-		} catch (error) {
-			const message = error instanceof Error ? error.message : String(error);
-			diagnostics.push({
-				type: "error",
-				message: `Extension "${extensionPath}" error: ${message}`,
-			});
-		}
-	}
-	extensionsResult.runtime.pendingNativeProviderRegistrations = [];
-	await modelRuntime.refresh({ allowNetwork: false });
-	diagnostics.push(...applyExtensionFlagValues(resourceLoader, options.extensionFlagValues));
-
-	return {
+	const services: AgentSessionServices = {
 		cwd,
 		agentDir,
 		modelRuntime,
 		settingsManager,
 		resourceLoader,
-		diagnostics,
+		diagnostics: [],
 	};
+	flushExtensionProviders(services);
+	await modelRuntime.refresh({ allowNetwork: false });
+	services.diagnostics.push(...applyExtensionFlagValues(resourceLoader, options.extensionFlagValues));
+	return services;
 }
 
 /**
diff --git a/packages/coding-agent/src/core/extensions/index.ts b/packages/coding-agent/src/core/extensions/index.ts
index f19816a..77bf7f9 100644
--- a/packages/coding-agent/src/core/extensions/index.ts
+++ b/packages/coding-agent/src/core/extensions/index.ts
@@ -105,6 +105,8 @@ export type {
 	MessageUpdateEvent,
 	ModelSelectEvent,
 	ModelSelectSource,
+	ModelBootstrapHandler,
+	ModelBootstrapRequest,
 	PowerShellToolCallEvent,
 	PowerShellToolResultEvent,
 	ProjectTrustContext,
diff --git a/packages/coding-agent/src/core/extensions/loader.ts b/packages/coding-agent/src/core/extensions/loader.ts
index 32ca6c9..4fac4c4 100644
--- a/packages/coding-agent/src/core/extensions/loader.ts
+++ b/packages/coding-agent/src/core/extensions/loader.ts
@@ -277,6 +277,11 @@ function createExtensionAPI(
 
 	const api = {
 		// Registration methods - write to extension
+		registerModelBootstrap(handler): void {
+			assertActive();
+			extension.modelBootstraps.push(handler);
+		},
+
 		on(event: string, handler: HandlerFn): void {
 			assertActive();
 			const list = extension.handlers.get(event) ?? [];
@@ -531,6 +536,7 @@ function createExtension(extensionPath: string, resolvedPath: string): Extension
 		resolvedPath,
 		sourceInfo: createSyntheticSourceInfo(extensionPath, { source, baseDir }),
 		handlers: new Map(),
+		modelBootstraps: [],
 		tools: new Map(),
 		messageRenderers: new Map(),
 		entryRenderers: new Map(),
diff --git a/packages/coding-agent/src/core/extensions/types.ts b/packages/coding-agent/src/core/extensions/types.ts
index eaff3a8..ebeef61 100644
--- a/packages/coding-agent/src/core/extensions/types.ts
+++ b/packages/coding-agent/src/core/extensions/types.ts
@@ -1304,6 +1304,14 @@ export interface ExtensionAPI {
 	// Tool Registration
 	// =========================================================================
 
+	/**
+	 * Register an awaited, provider-only bootstrap used before initial model
+	 * resolution. The handler may only register providers; normal session actions
+	 * are not initialized yet. Requests without a provider are deliberately
+	 * non-exact and handlers should ignore them rather than expanding a catalogue.
+	 */
+	registerModelBootstrap(handler: ModelBootstrapHandler): void;
+
 	/** Register a tool that the LLM can call. */
 	registerTool<TParams extends TSchema = TSchema, TDetails = unknown, TState = any>(
 		tool: ToolDefinition<TParams, TDetails, TState>,
@@ -1584,6 +1592,17 @@ export interface ProviderModelConfig {
 	compat?: Model<Api>["compat"];
 }
 
+export interface ModelBootstrapRequest {
+	/** Why Pi is resolving a model. `list` requests a bounded health/list seed. */
+	source: "cli" | "session" | "default" | "list";
+	/** Exact provider identity, absent for a bare/fuzzy CLI model pattern or list. */
+	provider?: string;
+	/** Exact model identity when provider is present; otherwise the unparsed CLI pattern. */
+	modelId?: string;
+}
+
+export type ModelBootstrapHandler = (request: Readonly<ModelBootstrapRequest>) => void | Promise<void>;
+
 /** Extension factory function type. Supports both sync and async initialization. */
 export type ExtensionFactory = (pi: ExtensionAPI) => void | Promise<void>;
 
@@ -1768,6 +1787,7 @@ export interface Extension {
 	hidden?: boolean;
 	sourceInfo: SourceInfo;
 	handlers: Map<string, HandlerFn[]>;
+	modelBootstraps: ModelBootstrapHandler[];
 	tools: Map<string, RegisteredTool>;
 	messageRenderers: Map<string, MessageRenderer>;
 	markdownTransformer?: MarkdownTransformer;
diff --git a/packages/coding-agent/src/main.ts b/packages/coding-agent/src/main.ts
index 55351df..55911a2 100644
--- a/packages/coding-agent/src/main.ts
+++ b/packages/coding-agent/src/main.ts
@@ -37,13 +37,14 @@ import { APP_NAME, ENV_SESSION_DIR, expandTildePath, getAgentDir, getPackageDir,
 import { type CreateAgentSessionRuntimeFactory, createAgentSessionRuntime } from "./core/agent-session-runtime.ts";
 import {
 	type AgentSessionRuntimeDiagnostic,
+	bootstrapExtensionModels,
 	createAgentSessionFromServices,
 	createAgentSessionServices,
 } from "./core/agent-session-services.ts";
 import { formatNoModelsAvailableMessage } from "./core/auth-guidance.ts";
 import { AuthStorage, ReadOnlyAuthStorage } from "./core/auth-storage.ts";
 import { exportFromFile } from "./core/export-html/index.ts";
-import type { InlineExtension } from "./core/extensions/types.ts";
+import type { InlineExtension, ModelBootstrapRequest } from "./core/extensions/types.ts";
 import { applyHttpProxySettings, configureHttpDispatcher } from "./core/http-dispatcher.ts";
 import { resolveCliModel, resolveModelScope, type ScopedModel } from "./core/model-resolver.ts";
 import { ModelRuntime } from "./core/model-runtime.ts";
@@ -443,6 +444,35 @@ export async function createSessionManager(
 	return SessionManager.create(cwd, sessionDir, { id: parsed.sessionId });
 }
 
+function requestedModelBootstrap(
+	parsed: Args,
+	sessionManager: SessionManager,
+	settingsManager: SettingsManager,
+): ModelBootstrapRequest | undefined {
+	if (parsed.listModels !== undefined) return { source: "list" };
+	if (parsed.model) {
+		if (parsed.provider) return { source: "cli", provider: parsed.provider, modelId: parsed.model };
+		const slash = parsed.model.indexOf("/");
+		if (slash > 0 && slash < parsed.model.length - 1) {
+			return {
+				source: "cli",
+				provider: parsed.model.slice(0, slash),
+				modelId: parsed.model.slice(slash + 1),
+			};
+		}
+		// Preserve ordinary Pi fuzzy matching, but do not invite an extension to
+		// materialize an unbounded catalogue for a bare pattern.
+		return { source: "cli", modelId: parsed.model };
+	}
+	const existing = sessionManager.buildSessionContext();
+	if (existing.messages.length > 0 && existing.model) {
+		return { source: "session", provider: existing.model.provider, modelId: existing.model.modelId };
+	}
+	const provider = settingsManager.getDefaultProvider();
+	const modelId = settingsManager.getDefaultModel();
+	return provider && modelId ? { source: "default", provider, modelId } : undefined;
+}
+
 function buildSessionOptions(
 	parsed: Args,
 	scopedModels: ScopedModel[],
@@ -775,6 +805,8 @@ export async function main(args: string[], options?: MainOptions) {
 			},
 		});
 		const { settingsManager, modelRuntime, resourceLoader } = services;
+		const bootstrapRequest = requestedModelBootstrap(parsed, sessionManager, settingsManager);
+		if (bootstrapRequest) await bootstrapExtensionModels(services, bootstrapRequest);
 		const diagnostics: AgentSessionRuntimeDiagnostic[] = [
 			...projectTrustDiagnostics,
 			...services.diagnostics,
