models
state
# Local configuration may contain credentials. The legacy .env name remains
# ignored defensively, but Familiar no longer reads it.
familiar.toml
.env

# Transient research/design/settlement/verdict artifacts
# (should be relocated to state/artifacts, not committed to repo root)
RESEARCH-*.md
VERDICT-*.md
SETTLEMENT-*.md
FINDINGS-*.md
