# Public-readiness inventory

This inventory records private-project terminology and personal/deployment identifiers intentionally left after the `rewrite` public-readiness scrub. Line numbers describe this revision. The branch's genuinely new trees (`agents/`, `packages/`, `services/server/`, `services/presence/`, `integrations/pi/extensions/agents/`, and `RESTRUCTURE-NOTES.md`) were searched separately and contain none of the audited identifiers.

The entries below predate `rewrite` (confirmed with rename-aware diffs and `git log --follow`) or are protected/behavior-bearing values. They were not mass-renamed because doing so would risk compatibility or violate the scrub constraints.

| Remaining instance(s) | What it reveals | Why left | Suggested future remedy |
|---|---|---|---|
| `README.md:29` | Public GitHub account/repository URL (`gisikw`) | This is the functional clone URL for the already-public repository, not private infrastructure. | Replace with the final organization URL if ownership changes. |
| `apps/desktop/package.json:14` | Reverse-domain app identifier containing `gisi` | Changing `appId` can change signing/update identity and installed application data behavior. | Plan an explicit desktop application-ID migration. |
| `integrations/pi/extensions/CLAUDE-DRIVER.md:1,3,9,17,19,30,53,62,69,76,297,299,313,325` | Retired private project name (`tiamat`) and one `/home/dev/.herdr` example | Historical/operational terminology for the pre-existing driver migration; rewriting it alongside code could obscure compatibility constraints. | Write a terminology migration spec, then update docs, implementation namespaces, fixtures, and snapshots together. |
| `integrations/pi/extensions/claude-driver/index.ts:1,5,6,59,187` | `tiamat` migration/provenance terminology | Comments describe fallback and parity behavior in working extension internals. | Rename as part of the coordinated driver terminology migration. |
| `integrations/pi/extensions/lib/anthropic-body.ts:3,6` | `tiamat` source provenance | Documents exact semantics of a compatibility port. | Replace with a public compatibility-spec citation after one exists. |
| `integrations/pi/extensions/lib/anthropic-body.test.ts:70,74` | `tiamat` test label and synthetic hostname | Existing regression fixture for inherited-route scrubbing. | Rename fixture and description together with the route migration. |
| `integrations/pi/extensions/lib/claude-cache-hygiene.ts:1,14`; `integrations/pi/extensions/lib/claude-cache-hygiene.test.ts:1` | `tiamat` port provenance | Existing wire-compatibility implementation and test. | Cite a public wire-format spec and rename in one tested change. |
| `integrations/pi/extensions/lib/claude-projection.ts:1,15,82,86,94,104,130,140,181,187,388,428,520` | `tiamat` provenance, deterministic namespace strings, and `/home/dev/.herdr` example | Several strings feed deterministic UUIDs; changing them breaks serialized compatibility. | Version the projection format/UUID namespace, migrate persisted data, then neutralize comments and examples. |
| `integrations/pi/extensions/lib/claude-projection.test.ts:1,21,45,55,61,62,81,88,97,107,121,123,152,207,208` | `tiamat` names, `/home/dev` paths, tool IDs, provenance fixtures | Golden compatibility fixtures are coupled to the deterministic projection contract. | Replace only with a versioned projection migration and regenerated goldens. |
| `integrations/pi/extensions/lib/claude-runner.ts:78` | `tiamat` routing terminology | Security-relevant inherited-environment scrub comment. | Rename after the fallback route is renamed. |
| `integrations/pi/extensions/lib/e2e-tools.test-harness.ts:22` | `tiamat` routing terminology | Existing e2e setup for route isolation. | Rename with the coordinated route migration. |
| `integrations/pi/extensions/lib/ratelimit-headers.ts:2` | `tiamat` source provenance | Existing behavior-port comment. | Cite a public protocol/spec instead. |
| `integrations/pi/extensions/subagent/native-agent-args.ts:17,27,46`; `integrations/pi/extensions/subagent/native-agent-args.test.ts:40` | `tiamat` fallback-route terminology | Describes load-order authority that is behaviorally significant. | Rename provider/fallback terminology and tests atomically. |
| `integrations/pi/extensions/subagent/route-authority.test.ts:14,19,21,63,82,112,113,118,119,121,126,127,131,136,137,145` | `tiamat` synthetic route, symbols, and test descriptions | Regression test proves provider authority and fallback behavior; broad renaming is risky. | Rename fixture symbols/descriptions during the provider migration. |
| `integrations/pi/extensions/subagent/index.ts:43,57,316` | Personal first name in orchestration comments | Pre-existing agent-system policy context; inventoried rather than mixed into functional repairs. | Rewrite these comments generically in a focused agent-policy review. |
| `integrations/pi/extensions/worklist/index.ts:639,651`; `integrations/pi/extensions/worklist/policy.ts:135` | Personal first name in user-facing/policy language | Functional conversational policy/config explicitly excluded from this scrub. | Make the operator display name configurable and use a neutral fallback. |
| `integrations/pi/extensions/web/index.ts:96,106` | Generic tailnet terminology and CGNAT classification | This is functional private-network request filtering, not a specific tailnet/topology. | Rename comments to generic overlay-network terminology without changing address classification. |
| `scripts/herdr-sidebar.sh:7` | Personal first name in provenance comment | Pre-existing validation provenance; unrelated to branch changes. | Replace with “validated” in a focused shell-script cleanup. |
| `skills/herdr/SKILL.md:68,70`; `skills/herdr/reference/agents.md:17` | Personal first name in agent-use guidance | `skills/` was explicitly out of bounds for this scrub. | Review and parameterize skill prose separately. |

## Scrub summary

- **Tier 1 (new branch content):** 0 instances found, 0 fixed. Rename-aware review found no audited PII/private-infrastructure identifiers in genuinely new content.
- **Tier 2 (cheap wins):** 25 flagged lines across 11 files neutralized. This removed the private hostname/auth topology and non-functional personal-name comments/provenance. The desktop fallback is now the local gateway (`http://localhost:1692`); `FAMILIAR_BASE_URL` and user config still take precedence.
- **Tier 3 (inventory):** 91 remaining matching lines across 23 files are listed above. This includes 73 `tiamat` lines, compatibility fixtures/paths, protected policy/skill prose, a stable desktop app ID, the public clone URL, and generic tailnet filtering terminology; categories overlap on some lines.
