diff --git a/src/app/actions.rs b/src/app/actions.rs
index a45687b4..82fbdda4 100644
--- a/src/app/actions.rs
+++ b/src/app/actions.rs
@@ -1564,10 +1564,7 @@ impl AppState {
             return;
         }
 
-        let (_, detail_area) = crate::ui::expanded_sidebar_sections(
-            self.view.sidebar_rect,
-            self.sidebar_section_split,
-        );
+        let (_, detail_area) = crate::ui::expanded_sidebar_sections(self, self.view.sidebar_rect);
         self.agent_panel_scroll = crate::ui::agent_panel_scroll_for_target(
             self,
             detail_area,
diff --git a/src/app/api.rs b/src/app/api.rs
index 92d9596f..644a6bb9 100644
--- a/src/app/api.rs
+++ b/src/app/api.rs
@@ -217,6 +217,15 @@ impl App {
                 self.close_popup_pane();
                 return Vec::new();
             }
+            if self
+                .state
+                .sidebar_pty
+                .as_ref()
+                .is_some_and(|pty| pty.pane_id == *pane_id)
+            {
+                self.close_sidebar_pty();
+                return Vec::new();
+            }
             let previous_toast = self.state.toast.clone();
             if let Some(update) = self.state.publish_pane_process_exit_if_agent(*pane_id) {
                 self.sync_full_lifecycle_authority_detection_pauses();
diff --git a/src/app/input/mouse.rs b/src/app/input/mouse.rs
index 3c6c86f4..aa8c3788 100644
--- a/src/app/input/mouse.rs
+++ b/src/app/input/mouse.rs
@@ -191,6 +191,15 @@ impl AppState {
             && mouse.row >= sidebar.y
             && mouse.row < sidebar.y + sidebar.height;
 
+        // The sidebar pty has no native click semantics: every mouse event
+        // inside its grid is forwarded verbatim to the child and consumed.
+        if in_sidebar
+            && matches!(self.mode, Mode::Terminal | Mode::Navigate | Mode::Resize)
+            && self.forward_sidebar_pty_mouse(terminal_runtimes, mouse)
+        {
+            return None;
+        }
+
         if self.handle_right_click_passthrough(terminal_runtimes, source_id, mouse, in_sidebar) {
             return None;
         }
@@ -1774,6 +1783,51 @@ impl AppState {
         )
     }
 
+    /// Forward a mouse event inside the sidebar pty grid to its child via the
+    /// terminal mouse protocols. Returns true when the event was inside the
+    /// grid and is consumed, even if the child has mouse reporting disabled —
+    /// the process inside owns all behavior, so nothing else may react.
+    fn forward_sidebar_pty_mouse(
+        &self,
+        terminal_runtimes: &TerminalRuntimeRegistry,
+        mouse: MouseEvent,
+    ) -> bool {
+        let Some(pty) = self.sidebar_pty.as_ref() else {
+            return false;
+        };
+        let Some(grid) = crate::ui::sidebar_pty_grid_rect(self, self.view.sidebar_rect) else {
+            return false;
+        };
+        if !rect_contains(grid, mouse.column, mouse.row) {
+            return false;
+        }
+        let Some(rt) = terminal_runtimes.get(&pty.terminal_id) else {
+            return true;
+        };
+        let Some(position) = self.pane_mouse_position(rt, grid, mouse) else {
+            return true;
+        };
+        let bytes = match mouse.kind {
+            MouseEventKind::Down(_) | MouseEventKind::Up(_) | MouseEventKind::Drag(_) => {
+                rt.encode_mouse_button(mouse.kind, position, mouse.modifiers)
+            }
+            MouseEventKind::Moved => rt.encode_mouse_motion(mouse.kind, position, mouse.modifiers),
+            MouseEventKind::ScrollUp
+            | MouseEventKind::ScrollDown
+            | MouseEventKind::ScrollLeft
+            | MouseEventKind::ScrollRight => {
+                rt.encode_mouse_wheel(mouse.kind, position, mouse.modifiers)
+            }
+        };
+        let Some(bytes) = bytes else {
+            return true;
+        };
+        if let Err(err) = rt.try_send_bytes(Bytes::from(bytes)) {
+            warn!(err = %err, kind = ?mouse.kind, "failed to forward sidebar pty mouse event");
+        }
+        true
+    }
+
     pub(super) fn forward_pane_mouse_button(
         &self,
         terminal_runtimes: &TerminalRuntimeRegistry,
diff --git a/src/app/input/navigate.rs b/src/app/input/navigate.rs
index 2cb657a7..6e62b31e 100644
--- a/src/app/input/navigate.rs
+++ b/src/app/input/navigate.rs
@@ -415,6 +415,10 @@ impl App {
                 self.state.sidebar_collapsed = !self.state.sidebar_collapsed;
                 leave_navigate_mode(&mut self.state);
             }
+            NavigateAction::ToggleSidebarFill => {
+                self.state.toggle_sidebar_pty_fill();
+                leave_navigate_mode(&mut self.state);
+            }
             NavigateAction::CyclePaneNext => {
                 self.cycle_pane_via_api(false);
                 leave_navigate_mode(&mut self.state);
@@ -1422,6 +1426,7 @@ pub(crate) enum NavigateAction {
     ResizePaneUp,
     ResizePaneRight,
     ToggleSidebar,
+    ToggleSidebarFill,
     CyclePaneNext,
     CyclePanePrevious,
     LastPane,
@@ -1572,6 +1577,7 @@ fn non_indexed_action_for_key(
         (&kb.resize_pane_up, NavigateAction::ResizePaneUp),
         (&kb.resize_pane_right, NavigateAction::ResizePaneRight),
         (&kb.toggle_sidebar, NavigateAction::ToggleSidebar),
+        (&kb.toggle_sidebar_fill, NavigateAction::ToggleSidebarFill),
         (&kb.reload_config, NavigateAction::ReloadConfig),
         (
             &kb.open_notification_target,
@@ -1830,6 +1836,10 @@ pub(super) fn execute_navigate_action_in_context(
             state.sidebar_collapsed = !state.sidebar_collapsed;
             leave_navigate_mode(state);
         }
+        NavigateAction::ToggleSidebarFill => {
+            state.toggle_sidebar_pty_fill();
+            leave_navigate_mode(state);
+        }
         NavigateAction::CyclePaneNext => {
             state.cycle_pane(false);
             leave_navigate_mode(state);
@@ -2389,6 +2399,27 @@ mod tests {
         assert_eq!(state.selected, 2);
     }
 
+    #[test]
+    fn sidebar_fill_toggle_key_flips_runtime_flag_and_exits_navigate() {
+        let mut state = state_with_workspaces(&["test"]);
+        state.keybinds.toggle_sidebar_fill = crate::config::ActionKeybinds::prefix("g");
+        assert!(!state.sidebar_pty_fill);
+
+        handle_navigate_key(
+            &mut state,
+            KeyEvent::new(KeyCode::Char('g'), KeyModifiers::empty()),
+        );
+        assert!(state.sidebar_pty_fill);
+        assert_eq!(state.mode, Mode::Terminal);
+
+        // Toggling again returns to the ordinary layout without a restart.
+        handle_navigate_key(
+            &mut state,
+            KeyEvent::new(KeyCode::Char('g'), KeyModifiers::empty()),
+        );
+        assert!(!state.sidebar_pty_fill);
+    }
+
     #[test]
     fn custom_sidebar_toggle_key_toggles_and_exits_navigate() {
         let mut state = state_with_workspaces(&["test"]);
diff --git a/src/app/input/sidebar.rs b/src/app/input/sidebar.rs
index 0923eef6..68f28156 100644
--- a/src/app/input/sidebar.rs
+++ b/src/app/input/sidebar.rs
@@ -10,7 +10,7 @@ impl AppState {
         if self.sidebar_collapsed || sidebar.width <= 1 || sidebar.height == 0 {
             return Rect::default();
         }
-        crate::ui::workspace_list_rect(sidebar, self.sidebar_section_split)
+        crate::ui::workspace_list_rect(self, sidebar)
     }
 
     pub(super) fn agent_panel_rect(&self) -> Rect {
@@ -18,8 +18,7 @@ impl AppState {
         if self.sidebar_collapsed || sidebar.width <= 1 || sidebar.height == 0 {
             return Rect::default();
         }
-        let (_, detail_area) =
-            crate::ui::expanded_sidebar_sections(sidebar, self.sidebar_section_split);
+        let (_, detail_area) = crate::ui::expanded_sidebar_sections(self, sidebar);
         detail_area
     }
 
@@ -276,10 +275,7 @@ impl AppState {
         if self.sidebar_collapsed {
             return false;
         }
-        let rect = crate::ui::sidebar_section_divider_rect(
-            self.view.sidebar_rect,
-            self.sidebar_section_split,
-        );
+        let rect = crate::ui::sidebar_section_divider_rect(self, self.view.sidebar_rect);
         rect.width > 0
             && col >= rect.x
             && col < rect.x + rect.width
@@ -289,11 +285,12 @@ impl AppState {
 
     pub(super) fn set_sidebar_section_split(&mut self, row: u16) {
         let sidebar = self.view.sidebar_rect;
-        let content_height = sidebar.height;
+        let pty_h = crate::ui::sidebar_pty_section_height(self, sidebar);
+        let content_height = sidebar.height.saturating_sub(pty_h);
         if content_height < 6 {
             return;
         }
-        let relative_y = row.saturating_sub(sidebar.y);
+        let relative_y = row.saturating_sub(sidebar.y.saturating_add(pty_h));
         let ratio = (relative_y as f32) / (content_height as f32);
         self.sidebar_section_split = ratio.clamp(0.1, 0.9);
         self.mark_session_dirty();
@@ -470,10 +467,7 @@ impl AppState {
             return false;
         }
 
-        let (_, detail_area) = crate::ui::expanded_sidebar_sections(
-            self.view.sidebar_rect,
-            self.sidebar_section_split,
-        );
+        let (_, detail_area) = crate::ui::expanded_sidebar_sections(self, self.view.sidebar_rect);
         let rect = crate::ui::agent_panel_toggle_rect(detail_area, self.agent_panel_sort);
         rect.width > 0
             && col >= rect.x
@@ -852,10 +846,8 @@ mod tests {
         app.state.mode = Mode::Terminal;
         app.state.agent_panel_scroll = 3;
 
-        let (_, detail_area) = crate::ui::expanded_sidebar_sections(
-            app.state.view.sidebar_rect,
-            app.state.sidebar_section_split,
-        );
+        let (_, detail_area) =
+            crate::ui::expanded_sidebar_sections(&app.state, app.state.view.sidebar_rect);
         let toggle = crate::ui::agent_panel_toggle_rect(detail_area, app.state.agent_panel_sort);
         app.handle_mouse(mouse(
             MouseEventKind::Down(MouseButton::Left),
@@ -898,10 +890,8 @@ mod tests {
         app.state.selected = 0;
         app.state.mode = Mode::Terminal;
 
-        let (_, detail_area) = crate::ui::expanded_sidebar_sections(
-            app.state.view.sidebar_rect,
-            app.state.sidebar_section_split,
-        );
+        let (_, detail_area) =
+            crate::ui::expanded_sidebar_sections(&app.state, app.state.view.sidebar_rect);
         app.handle_mouse(mouse(
             MouseEventKind::Down(MouseButton::Left),
             detail_area.x + 2,
@@ -1894,10 +1884,8 @@ mod tests {
     #[test]
     fn dragging_sidebar_section_divider_sets_split_ratio() {
         let mut app = app_for_mouse_test();
-        let divider = crate::ui::sidebar_section_divider_rect(
-            app.state.view.sidebar_rect,
-            app.state.sidebar_section_split,
-        );
+        let divider =
+            crate::ui::sidebar_section_divider_rect(&app.state, app.state.view.sidebar_rect);
 
         app.handle_mouse(mouse(
             MouseEventKind::Down(MouseButton::Left),
diff --git a/src/app/mod.rs b/src/app/mod.rs
index 85ad4fa3..23d899b5 100644
--- a/src/app/mod.rs
+++ b/src/app/mod.rs
@@ -22,6 +22,7 @@ mod popup;
 mod runtime;
 mod runtime_mutations;
 mod session;
+mod sidebar_pty;
 pub mod state;
 mod tab_bar_status;
 mod terminal_targets;
@@ -693,6 +694,8 @@ impl App {
             installed_plugins: load_plugin_registry(no_session),
             plugin_panes: std::collections::HashMap::new(),
             popup_pane: None,
+            sidebar_pty: None,
+            sidebar_pty_fill: false,
             plugin_command_logs: Vec::new(),
             next_plugin_command_log_id: 1,
             plugin_commands_in_flight: 0,
@@ -876,6 +879,9 @@ impl App {
                 .get(idx)
                 .and_then(|ws| ws.focused_pane_id().map(|pane_id| (idx, pane_id)))
         });
+        // The sidebar pty is never part of handoff snapshots; respawn it from
+        // config after the imported terminals and runtimes are installed.
+        app.spawn_sidebar_pty(&config.ui.sidebar.pty);
         Ok(app)
     }
 
@@ -1518,6 +1524,8 @@ impl App {
                 }
                 self.state.sound = config.ui.sound.clone();
                 self.state.toast_config = config.ui.toast.clone();
+                self.close_sidebar_pty();
+                self.spawn_sidebar_pty(&config.ui.sidebar.pty);
             }
         }
 
diff --git a/src/app/sidebar_pty.rs b/src/app/sidebar_pty.rs
new file mode 100644
index 00000000..7d610950
--- /dev/null
+++ b/src/app/sidebar_pty.rs
@@ -0,0 +1,209 @@
+use std::path::PathBuf;
+
+use tracing::warn;
+
+use crate::app::App;
+use crate::layout::PaneId;
+use crate::pane::PaneLaunchEnv;
+use crate::terminal::{TerminalId, TerminalRuntime, TerminalState};
+
+fn default_sidebar_pty_cwd() -> PathBuf {
+    #[cfg(windows)]
+    const HOME_VAR: &str = "USERPROFILE";
+    #[cfg(unix)]
+    const HOME_VAR: &str = "HOME";
+    std::env::var_os(HOME_VAR)
+        .map(PathBuf::from)
+        .unwrap_or_else(|| std::env::current_dir().unwrap_or_else(|_| PathBuf::from("/")))
+}
+
+impl App {
+    /// Spawn the embedded sidebar terminal from config. A no-op when the
+    /// command is unset or a sidebar pty is already running; spawn failures
+    /// leave the section disabled instead of failing startup.
+    pub(crate) fn spawn_sidebar_pty(&mut self, config: &crate::config::SidebarPtyConfig) {
+        let Some(command) = config.enabled_command() else {
+            return;
+        };
+        // Seed the runtime fill flag from config on every spawn. A runtime
+        // toggle only lives until the next spawn/reload, matching the
+        // "ephemeral across restart" contract.
+        self.state.sidebar_pty_fill = config.fill;
+        if self.state.sidebar_pty.is_some() {
+            return;
+        }
+        let cwd = config
+            .cwd
+            .clone()
+            .map(PathBuf::from)
+            .unwrap_or_else(default_sidebar_pty_cwd);
+        let cols = crate::ui::sidebar_pty_grid_cols(self.state.sidebar_width);
+        let rows = crate::ui::sidebar_pty_rows_for_width(cols, config.rows);
+        let pane_id = PaneId::alloc();
+        let terminal_id = TerminalId::alloc();
+        let launch_env = PaneLaunchEnv::from_extra(Vec::new()).without_pane_identity();
+        let runtime = TerminalRuntime::spawn_shell_command(
+            pane_id,
+            rows,
+            cols,
+            cwd.clone(),
+            command,
+            &launch_env,
+            crate::pane::AgentDetection::Disabled,
+            self.state.pane_scrollback_limit_bytes,
+            self.state.host_terminal_theme,
+            self.state.host_terminal_appearance,
+            self.event_tx.clone(),
+            self.render_notify.clone(),
+            self.render_dirty.clone(),
+        );
+        match runtime {
+            Ok(runtime) => {
+                self.terminal_runtimes.insert(terminal_id.clone(), runtime);
+                self.state.terminals.insert(
+                    terminal_id.clone(),
+                    TerminalState::new(terminal_id.clone(), cwd),
+                );
+                self.state.sidebar_pty = Some(crate::app::state::SidebarPtyState {
+                    pane_id,
+                    terminal_id,
+                    rows_override: config.rows,
+                });
+            }
+            Err(err) => {
+                warn!(command, err = %err, "failed to spawn sidebar pty command");
+            }
+        }
+    }
+
+    pub(crate) fn close_sidebar_pty(&mut self) -> bool {
+        let Some(pty) = self.state.sidebar_pty.take() else {
+            return false;
+        };
+        self.state.terminals.remove(&pty.terminal_id);
+        self.shutdown_terminal_runtime(pty.terminal_id);
+        self.render_dirty.request_generic();
+        self.render_notify.notify_one();
+        true
+    }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    fn test_app() -> App {
+        let (_api_tx, api_rx) = tokio::sync::mpsc::unbounded_channel();
+        App::new(
+            &crate::config::Config::default(),
+            true,
+            None,
+            api_rx,
+            crate::api::EventHub::default(),
+        )
+    }
+
+    fn install_test_sidebar_pty(app: &mut App) -> (PaneId, TerminalId) {
+        let pane_id = PaneId::alloc();
+        let terminal_id = TerminalId::alloc();
+        app.state.terminals.insert(
+            terminal_id.clone(),
+            TerminalState::new(terminal_id.clone(), PathBuf::from("/sidebar-pty")),
+        );
+        app.state.sidebar_pty = Some(crate::app::state::SidebarPtyState {
+            pane_id,
+            terminal_id: terminal_id.clone(),
+            rows_override: None,
+        });
+        (pane_id, terminal_id)
+    }
+
+    #[test]
+    fn spawn_is_a_no_op_without_a_command() {
+        let mut app = test_app();
+
+        app.spawn_sidebar_pty(&crate::config::SidebarPtyConfig::default());
+
+        assert!(app.state.sidebar_pty.is_none());
+        assert!(app.state.terminals.is_empty());
+        assert!(!app.state.sidebar_pty_fill);
+    }
+
+    #[test]
+    fn spawn_without_a_command_does_not_seed_fill() {
+        // Fill requested but no command: nothing spawns and the runtime flag
+        // stays off, so the sidebar cannot claim a full rectangle it has no
+        // pty to fill.
+        let mut app = test_app();
+        let config = crate::config::SidebarPtyConfig {
+            fill: true,
+            ..Default::default()
+        };
+
+        app.spawn_sidebar_pty(&config);
+
+        assert!(app.state.sidebar_pty.is_none());
+        assert!(!app.state.sidebar_pty_fill);
+    }
+
+    #[test]
+    fn toggle_fill_flips_runtime_flag_both_directions() {
+        let mut app = test_app();
+        assert!(!app.state.sidebar_pty_fill);
+
+        app.state.toggle_sidebar_pty_fill();
+        assert!(app.state.sidebar_pty_fill);
+
+        app.state.toggle_sidebar_pty_fill();
+        assert!(!app.state.sidebar_pty_fill);
+    }
+
+    #[test]
+    fn pane_death_clears_fill_target_so_sidebar_falls_back_to_nav() {
+        // Fill is on and a pty is live; when the pty dies the section clears and
+        // fill is no longer active, so rendering falls back to navigation
+        // instead of leaving a blank sidebar.
+        let mut app = test_app();
+        let (pane_id, _terminal_id) = install_test_sidebar_pty(&mut app);
+        app.state.sidebar_pty_fill = true;
+        assert!(crate::ui::sidebar_pty_fill_active(&app.state));
+
+        app.handle_internal_event(crate::events::AppEvent::PaneDied { pane_id });
+
+        assert!(app.state.sidebar_pty.is_none());
+        assert!(!crate::ui::sidebar_pty_fill_active(&app.state));
+    }
+
+    #[test]
+    fn close_removes_terminal_state_and_reports_closed() {
+        let mut app = test_app();
+        let (_pane_id, terminal_id) = install_test_sidebar_pty(&mut app);
+
+        assert!(app.close_sidebar_pty());
+
+        assert!(app.state.sidebar_pty.is_none());
+        assert!(!app.state.terminals.contains_key(&terminal_id));
+        assert!(!app.close_sidebar_pty());
+    }
+
+    #[test]
+    fn pane_died_for_sidebar_pty_clears_the_section() {
+        let mut app = test_app();
+        let (pane_id, terminal_id) = install_test_sidebar_pty(&mut app);
+
+        app.handle_internal_event(crate::events::AppEvent::PaneDied { pane_id });
+
+        assert!(app.state.sidebar_pty.is_none());
+        assert!(!app.state.terminals.contains_key(&terminal_id));
+    }
+
+    #[test]
+    fn sidebar_pty_survives_state_invariants_without_workspaces() {
+        let mut app = test_app();
+        install_test_sidebar_pty(&mut app);
+
+        app.state.assert_invariants_for_test();
+
+        assert!(app.state.sidebar_pty.is_some());
+    }
+}
diff --git a/src/app/state.rs b/src/app/state.rs
index d9dcfc9d..faea5fcf 100644
--- a/src/app/state.rs
+++ b/src/app/state.rs
@@ -25,6 +25,18 @@ pub(crate) struct PopupPaneState {
     pub height: Option<crate::popup_size::PopupSize>,
 }
 
+/// Embedded terminal pinned above the spaces section of the expanded sidebar.
+/// This is intentionally outside workspace layouts and never persisted;
+/// session restore respawns it from config.
+#[derive(Clone, Debug, PartialEq, Eq)]
+pub(crate) struct SidebarPtyState {
+    pub pane_id: PaneId,
+    pub terminal_id: crate::terminal::TerminalId,
+    /// Configured grid height override. None derives the height from the
+    /// sidebar width each frame to approximate a 4:3 visual aspect ratio.
+    pub rows_override: Option<u16>,
+}
+
 // ---------------------------------------------------------------------------
 // Selection autoscroll types
 // ---------------------------------------------------------------------------
@@ -1543,6 +1555,14 @@ pub struct AppState {
     pub(crate) plugin_panes: std::collections::HashMap<PaneId, PluginPaneRecord>,
     /// Session-modal terminal popup. This is intentionally outside workspace layouts.
     pub(crate) popup_pane: Option<PopupPaneState>,
+    /// Embedded terminal pinned at the top of the expanded sidebar.
+    pub(crate) sidebar_pty: Option<SidebarPtyState>,
+    /// Runtime fill-mode flag for the sidebar pty. Seeded from
+    /// `ui.sidebar.pty.fill` on spawn/reload and flipped by the
+    /// `toggle_sidebar_fill` action. Not persisted across restart. Only takes
+    /// effect while the sidebar pty is live; otherwise the sidebar falls back
+    /// to ordinary workspace/agent navigation.
+    pub(crate) sidebar_pty_fill: bool,
     /// Recent plugin action/event command executions.
     pub(crate) plugin_command_logs: Vec<crate::api::schema::PluginCommandLogInfo>,
     pub(crate) next_plugin_command_log_id: u64,
@@ -1567,6 +1587,16 @@ impl AppState {
         self.session_dirty = true;
     }
 
+    /// Flip the sidebar pty fill surface between full-rectangle fill and the
+    /// ordinary capped pty + workspace/agent navigation layout. Ephemeral: the
+    /// flag is re-seeded from `ui.sidebar.pty.fill` on the next spawn/reload
+    /// and is not persisted across restart. Toggling has no visible effect
+    /// while no sidebar pty is live, but the intent is still recorded so a
+    /// later spawn/reload does not silently drop it before config re-seeds.
+    pub(crate) fn toggle_sidebar_pty_fill(&mut self) {
+        self.sidebar_pty_fill = !self.sidebar_pty_fill;
+    }
+
     pub(crate) fn remove_alias_shadowed_by_new_pane(&mut self, pane_id: PaneId) {
         self.pane_id_aliases.remove(&pane_id.raw());
     }
@@ -1927,6 +1957,8 @@ impl AppState {
             installed_plugins: std::collections::HashMap::new(),
             plugin_panes: std::collections::HashMap::new(),
             popup_pane: None,
+            sidebar_pty: None,
+            sidebar_pty_fill: false,
             plugin_command_logs: Vec::new(),
             next_plugin_command_log_id: 1,
             plugin_commands_in_flight: 0,
@@ -2175,6 +2207,19 @@ impl AppState {
                 popup.terminal_id
             );
         }
+        if let Some(pty) = &self.sidebar_pty {
+            assert!(
+                self.terminals.contains_key(&pty.terminal_id),
+                "sidebar pty {:?} references missing terminal {}",
+                pty.pane_id,
+                pty.terminal_id
+            );
+            assert!(
+                !attached_terminal_ids.contains(&pty.terminal_id),
+                "sidebar pty terminal {} must not be attached to a tiled pane",
+                pty.terminal_id
+            );
+        }
         for &pane_id in self.plugin_panes.keys() {
             assert_live_pane(pane_id, "plugin pane record");
         }
diff --git a/src/config.rs b/src/config.rs
index 0595ae2f..ed6fabbd 100644
--- a/src/config.rs
+++ b/src/config.rs
@@ -28,7 +28,7 @@ pub use self::{
         UpdateChannelConfig, MAX_TOAST_DELAY_SECONDS,
     },
     sidebar::{
-        AgentSidebarToken, AgentsSidebarConfig, SidebarConfig, SidebarTokenStyle,
+        AgentSidebarToken, AgentsSidebarConfig, SidebarConfig, SidebarPtyConfig, SidebarTokenStyle,
         SpaceSidebarToken, SpacesSidebarConfig,
     },
     sound::SoundConfig,
diff --git a/src/config/keybinds.rs b/src/config/keybinds.rs
index 6cddd3ac..becc3e31 100644
--- a/src/config/keybinds.rs
+++ b/src/config/keybinds.rs
@@ -357,6 +357,7 @@ pub struct Keybinds {
     pub resize_pane_up: ActionKeybinds,
     pub resize_pane_right: ActionKeybinds,
     pub toggle_sidebar: ActionKeybinds,
+    pub toggle_sidebar_fill: ActionKeybinds,
     pub custom_commands: Vec<CustomCommandKeybind>,
 }
 
@@ -525,6 +526,7 @@ impl Config {
             resize_pane_up: empty_action!(),
             resize_pane_right: empty_action!(),
             toggle_sidebar: empty_action!(),
+            toggle_sidebar_fill: empty_action!(),
             custom_commands: Vec::new(),
         };
 
@@ -672,6 +674,11 @@ impl Config {
             apply_action!(keybinds.resize_pane_up, resize_pane_up, source);
             apply_action!(keybinds.resize_pane_right, resize_pane_right, source);
             apply_action!(keybinds.toggle_sidebar, toggle_sidebar, source);
+            apply_action!(
+                keybinds.toggle_sidebar_fill,
+                toggle_sidebar_fill,
+                source
+            );
 
             if source == field_source!(indexed) {
                 append_legacy_indexed_bindings(
diff --git a/src/config/model.rs b/src/config/model.rs
index c10cd733..e0a7133d 100644
--- a/src/config/model.rs
+++ b/src/config/model.rs
@@ -457,6 +457,10 @@ pub struct KeysConfig {
     pub resize_pane_right: BindingConfig,
     /// Toggle sidebar collapse. Default: "prefix+b"
     pub toggle_sidebar: BindingConfig,
+    /// Toggle the sidebar pty fill surface (full-rectangle sidebar pty vs.
+    /// the ordinary capped pty + workspace/agent navigation). No effect when
+    /// no sidebar pty is live. Default: "prefix+shift+b"
+    pub toggle_sidebar_fill: BindingConfig,
     /// Optional indexed shortcuts expanded over number keys 1-9.
     pub indexed: IndexedKeysConfig,
     /// Prefix-mode custom command bindings.
@@ -588,6 +592,8 @@ pub(crate) struct KeysConfigOverlay {
     #[serde(skip_serializing_if = "Option::is_none")]
     toggle_sidebar: Option<BindingConfig>,
     #[serde(skip_serializing_if = "Option::is_none")]
+    toggle_sidebar_fill: Option<BindingConfig>,
+    #[serde(skip_serializing_if = "Option::is_none")]
     indexed: Option<IndexedKeysConfig>,
     #[serde(skip_serializing)]
     command: Option<Vec<CommandKeybindConfig>>,
@@ -669,6 +675,7 @@ impl<'de> Deserialize<'de> for KeysConfig {
         apply_field!(resize_pane_up);
         apply_field!(resize_pane_right);
         apply_field!(toggle_sidebar);
+        apply_field!(toggle_sidebar_fill);
         apply_field!(indexed);
         apply_field!(command);
 
@@ -773,6 +780,7 @@ impl KeysConfig {
         copy_effective_action_field!(resize_pane_up, keybinds.resize_pane_up);
         copy_effective_action_field!(resize_pane_right, keybinds.resize_pane_right);
         copy_effective_action_field!(toggle_sidebar, keybinds.toggle_sidebar);
+        copy_effective_action_field!(toggle_sidebar_fill, keybinds.toggle_sidebar_fill);
         copy_user_field!(indexed);
 
         profile
@@ -1083,6 +1091,7 @@ impl Default for KeysConfig {
             resize_pane_up: BindingConfig::empty(),
             resize_pane_right: BindingConfig::empty(),
             toggle_sidebar: BindingConfig::one("prefix+b"),
+            toggle_sidebar_fill: BindingConfig::one("prefix+shift+b"),
             indexed: IndexedKeysConfig::default(),
             command: Vec::new(),
             user_fields: BTreeSet::new(),
diff --git a/src/config/sidebar.rs b/src/config/sidebar.rs
index c548c839..cd9856d2 100644
--- a/src/config/sidebar.rs
+++ b/src/config/sidebar.rs
@@ -424,11 +424,43 @@ impl Default for SpacesSidebarConfig {
     }
 }
 
+/// Embedded terminal pinned above the spaces section of the expanded sidebar.
+/// The section is entirely disabled while `command` is unset.
+#[derive(Debug, Clone, PartialEq, Eq, Default, Deserialize, Serialize)]
+#[serde(default)]
+pub struct SidebarPtyConfig {
+    /// Shell command spawned in the sidebar terminal. Unset disables the section.
+    pub command: Option<String>,
+    /// Fixed grid height override in rows. Default: derived from the sidebar
+    /// width to approximate a 4:3 visual aspect ratio.
+    pub rows: Option<u16>,
+    /// Working directory for the command. Default: the user's home directory.
+    pub cwd: Option<String>,
+    /// Fill mode: give the live sidebar pty the full expanded-sidebar
+    /// rectangle and hide the workspace/agent navigation while it is active.
+    /// Default off. When the pty is absent, fails to spawn, or dies, the
+    /// sidebar falls back to ordinary navigation even if this is set. This is
+    /// the startup default; it is runtime-toggleable via the
+    /// `toggle_sidebar_fill` action and the runtime value is not persisted.
+    #[serde(default)]
+    pub fill: bool,
+}
+
+impl SidebarPtyConfig {
+    pub(crate) fn enabled_command(&self) -> Option<&str> {
+        self.command
+            .as_deref()
+            .map(str::trim)
+            .filter(|command| !command.is_empty())
+    }
+}
+
 #[derive(Debug, Clone, PartialEq, Eq, Default, Deserialize, Serialize)]
 #[serde(default)]
 pub struct SidebarConfig {
     pub agents: AgentsSidebarConfig,
     pub spaces: SpacesSidebarConfig,
+    pub pty: SidebarPtyConfig,
 }
 
 #[cfg(test)]
@@ -635,6 +667,62 @@ rows = [[{ token = "git_status", fg = "#ff00aa" }], [{ token = "$jj", bold = tru
         assert_eq!(config.ui.sidebar.agents.rows_by_agent.len(), agents.len());
     }
 
+    #[test]
+    fn sidebar_pty_defaults_to_disabled() {
+        let config = SidebarConfig::default();
+        assert_eq!(config.pty, SidebarPtyConfig::default());
+        assert!(config.pty.enabled_command().is_none());
+        assert!(config.pty.rows.is_none());
+        assert!(config.pty.cwd.is_none());
+        assert!(!config.pty.fill);
+    }
+
+    #[test]
+    fn parses_sidebar_pty_table() {
+        let config: crate::config::Config = toml::from_str(
+            r#"
+[ui.sidebar.pty]
+command = "btop"
+rows = 8
+cwd = "/tmp"
+"#,
+        )
+        .expect("sidebar pty config");
+
+        assert_eq!(config.ui.sidebar.pty.enabled_command(), Some("btop"));
+        assert_eq!(config.ui.sidebar.pty.rows, Some(8));
+        assert_eq!(config.ui.sidebar.pty.cwd.as_deref(), Some("/tmp"));
+        assert!(!config.ui.sidebar.pty.fill);
+    }
+
+    #[test]
+    fn parses_sidebar_pty_fill_flag() {
+        let config: crate::config::Config = toml::from_str(
+            r#"
+[ui.sidebar.pty]
+command = "btop"
+fill = true
+"#,
+        )
+        .expect("sidebar pty config");
+
+        assert!(config.ui.sidebar.pty.fill);
+        assert_eq!(config.ui.sidebar.pty.enabled_command(), Some("btop"));
+    }
+
+    #[test]
+    fn blank_sidebar_pty_command_stays_disabled() {
+        let config: crate::config::Config = toml::from_str(
+            r#"
+[ui.sidebar.pty]
+command = "   "
+"#,
+        )
+        .expect("sidebar pty config");
+
+        assert!(config.ui.sidebar.pty.enabled_command().is_none());
+    }
+
     #[test]
     fn rejects_alias_case_whitespace_and_unknown_override_keys() {
         for key in ["claude-code", "Claude", "' claude '", "unknown"] {
diff --git a/src/kitty_graphics.rs b/src/kitty_graphics.rs
index 12141173..138b1218 100644
--- a/src/kitty_graphics.rs
+++ b/src/kitty_graphics.rs
@@ -287,6 +287,32 @@ pub(crate) fn has_visible_pane_graphics(
         return false;
     }
 
+    if let Some((pane_id, terminal_id, grid)) = sidebar_pty_placement_target(app) {
+        if let Some(runtime) = terminal_runtimes.get(&terminal_id) {
+            let scrollback_offset = runtime
+                .scroll_metrics()
+                .map(|m| m.offset_from_bottom as u32)
+                .unwrap_or(0);
+            for placement in runtime.kitty_image_placements_with_data_filter(|_| false) {
+                let host_placement = HostPlacement {
+                    pane_id,
+                    host_image_id: None,
+                    area: grid,
+                    cell_size,
+                    source_key: HostSourceKey::Terminal {
+                        pane_id,
+                        image_id: placement.image_id,
+                    },
+                    placement,
+                    scrollback_offset,
+                };
+                if clipped_placement(&host_placement).is_some() {
+                    return true;
+                }
+            }
+        }
+    }
+
     let Some(ws_idx) = app.active else {
         return false;
     };
@@ -962,6 +988,16 @@ fn active_view_key(app: &AppState) -> Option<HostViewKey> {
     })
 }
 
+/// Sidebar pty grid target for kitty graphics passthrough: the child's image
+/// placements blit into the grid rect like any tiled pane's placements.
+fn sidebar_pty_placement_target(
+    app: &AppState,
+) -> Option<(PaneId, crate::terminal::TerminalId, Rect)> {
+    let pty = app.sidebar_pty.as_ref()?;
+    let grid = crate::ui::sidebar_pty_grid_rect(app, app.view.sidebar_rect)?;
+    Some((pty.pane_id, pty.terminal_id.clone(), grid))
+}
+
 fn collect_visible_placements(
     app: &AppState,
     graphics: &crate::app::pane_graphics::Runtime,
@@ -970,11 +1006,40 @@ fn collect_visible_placements(
     cell_size: HostCellSize,
     uploaded_images: &HashMap<u32, ImageSignature>,
 ) -> Vec<HostPlacement> {
+    let mut placements = Vec::new();
+    if let Some((pane_id, terminal_id, grid)) = sidebar_pty_placement_target(app) {
+        if let Some(runtime) = terminal_runtimes.get(&terminal_id) {
+            let scrollback_offset = runtime
+                .scroll_metrics()
+                .map(|m| m.offset_from_bottom as u32)
+                .unwrap_or(0);
+            for placement in runtime.kitty_image_placements_with_data_filter(|descriptor| {
+                let format_code = kitty_format_code(descriptor.format);
+                let signature = image_signature_from_descriptor(descriptor, format_code);
+                let host_id = host_image_id_for_signature(pane_id, signature);
+                uploaded_images.get(&host_id).copied() != Some(signature)
+            }) {
+                placements.push(HostPlacement {
+                    pane_id,
+                    host_image_id: None,
+                    area: grid,
+                    cell_size,
+                    source_key: HostSourceKey::Terminal {
+                        pane_id,
+                        image_id: placement.image_id,
+                    },
+                    placement,
+                    scrollback_offset,
+                });
+            }
+        }
+    }
+
     let ws_idx = match app.active {
         Some(idx) => idx,
         None => {
             tracing::debug!("collect_visible_placements: no active workspace");
-            return Vec::new();
+            return placements;
         }
     };
     if app
@@ -984,7 +1049,7 @@ fn collect_visible_placements(
         .is_none()
     {
         tracing::debug!(ws_idx, "collect_visible_placements: no active tab");
-        return Vec::new();
+        return placements;
     }
 
     tracing::debug!(
@@ -993,7 +1058,6 @@ fn collect_visible_placements(
         pane_infos_len = surface.pane_infos.len(),
         "collect_visible_placements: starting iteration"
     );
-    let mut placements = Vec::new();
     for info in surface.pane_infos {
         let mut pane_layers = graphics
             .slots
diff --git a/src/main.rs b/src/main.rs
index 36b1d021..7e32e3c2 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -222,6 +222,7 @@ const DEFAULT_CONFIG: &str = r##"# herdr configuration
 # resize_pane_up = ""     # optional, e.g. "ctrl+shift+alt+up"
 # resize_pane_right = ""  # optional, e.g. "ctrl+shift+alt+right"
 # toggle_sidebar = "prefix+b"
+# toggle_sidebar_fill = "prefix+shift+b"   # full-rectangle sidebar pty vs. capped pty + navigation
 
 # Navigate-mode movement. These local shortcuts win while navigate mode is open.
 # They are independent from focus_pane_*. Do not include prefix+, esc, enter, tab, or 1..9 here.
@@ -894,6 +895,7 @@ fn main() -> io::Result<()> {
             api_rx,
             event_hub,
         );
+        app.spawn_sidebar_pty(&config.ui.sidebar.pty);
         let result = app.run(&mut terminal).await;
 
         // Reset modifyOtherKeys if we enabled it.
diff --git a/src/server/headless.rs b/src/server/headless.rs
index 9161cd34..b5bb6739 100644
--- a/src/server/headless.rs
+++ b/src/server/headless.rs
@@ -4177,6 +4177,15 @@ impl HeadlessServer {
         {
             return Some(&popup.terminal_id);
         }
+        if let Some(pty) = self
+            .app
+            .state
+            .sidebar_pty
+            .as_ref()
+            .filter(|pty| pty.pane_id == pane_id)
+        {
+            return Some(&pty.terminal_id);
+        }
         self.app
             .find_pane(pane_id)
             .map(|(_, pane)| &pane.attached_terminal_id)
@@ -4192,6 +4201,21 @@ impl HeadlessServer {
         {
             return true;
         }
+        if self
+            .app
+            .state
+            .sidebar_pty
+            .as_ref()
+            .is_some_and(|pty| pty.pane_id == pane_id)
+        {
+            // Visible only while the expanded sidebar shows the section; a
+            // collapsed or too-small sidebar keeps hidden-source skips intact.
+            return crate::ui::sidebar_pty_grid_rect(
+                &self.app.state,
+                self.app.state.view.sidebar_rect,
+            )
+            .is_some();
+        }
         let Some(workspace) = self
             .app
             .state
@@ -4314,6 +4338,38 @@ impl HeadlessServer {
             }
         }
 
+        if let Some(pty) = self.app.state.sidebar_pty.as_ref() {
+            if let Some(grid) =
+                crate::ui::sidebar_pty_grid_rect(&self.app.state, self.app.state.view.sidebar_rect)
+            {
+                if !rect_fits_frame(grid, &frame) {
+                    retained_fallback!("sidebar_pty_rect_outside_frame");
+                }
+                let Some(runtime) = self.app.terminal_runtimes.get(&pty.terminal_id) else {
+                    retained_fallback!("missing_runtime");
+                };
+                match runtime.collect_dirty_patch(grid.width, grid.height) {
+                    crate::pane::TerminalDirtyPatchOutcome::Clean => {
+                        crate::render_prof::event("retained.pane_clean");
+                    }
+                    crate::pane::TerminalDirtyPatchOutcome::Fallback => {
+                        retained_fallback!("dirty_patch_fallback");
+                    }
+                    crate::pane::TerminalDirtyPatchOutcome::Patch(patch) => {
+                        crate::render_prof::event("retained.pane_patch");
+                        crate::render_prof::counter("retained.patch_rows", patch.rows.len() as u64);
+                        if dirty_patch_intersects_hyperlinks(&frame, grid, &patch) {
+                            retained_fallback!("hyperlink_intersection");
+                        }
+                        if !apply_terminal_dirty_patch(&mut frame, grid, patch) {
+                            retained_fallback!("patch_apply_failed");
+                        }
+                        touched = true;
+                    }
+                }
+            }
+        }
+
         let previous_cursor = frame.cursor.clone();
         frame.cursor = crate::server::render_stream::focused_terminal_cursor(
             &self.app.state,
@@ -5095,6 +5151,7 @@ pub fn run_server() -> io::Result<()> {
             event_hub,
         );
         seed_startup_workspace_if_empty(&mut app);
+        app.spawn_sidebar_pty(&loaded_config.config.ui.sidebar.pty);
 
         // The server runs headless — disable local notification side effects.
         // Sound and terminal notifications are forwarded to connected clients
diff --git a/src/ui.rs b/src/ui.rs
index 8adc31ef..ac8532a7 100644
--- a/src/ui.rs
+++ b/src/ui.rs
@@ -52,6 +52,7 @@ pub(crate) use self::scrollbar::{
     scrollbar_offset_from_row, scrollbar_thumb_grab_offset, should_show_scrollbar,
 };
 use self::settings::render_settings_overlay;
+use self::sidebar::resize_sidebar_pty;
 #[cfg(test)]
 pub(crate) use self::sidebar::workspace_drop_indicator_row;
 use self::sidebar::{render_sidebar, render_sidebar_collapsed};
@@ -80,11 +81,13 @@ pub(crate) use self::{
         agent_panel_scroll_for_target, agent_panel_scroll_metrics, agent_panel_scrollbar_rect,
         agent_panel_toggle_rect, all_agent_panel_entries, collapsed_sidebar_sections,
         collapsed_sidebar_toggle_rect, compute_workspace_card_areas, expanded_sidebar_sections,
-        expanded_sidebar_toggle_rect, normalized_workspace_scroll, sidebar_section_divider_rect,
-        workspace_drop_slots, workspace_group_chevron_rect, workspace_list_entries,
-        workspace_list_entries_expanded, workspace_list_rect, workspace_list_scroll_metrics,
-        workspace_list_scrollbar_rect, workspace_parent_group_state, AgentPanelEntry,
-        WorkspaceListEntry,
+        expanded_sidebar_toggle_rect, normalized_workspace_scroll, sidebar_pty_fill_active,
+        sidebar_pty_grid_cols,
+        sidebar_pty_grid_rect, sidebar_pty_rows_for_width, sidebar_pty_section_height,
+        sidebar_section_divider_rect, workspace_drop_slots, workspace_group_chevron_rect,
+        workspace_list_entries, workspace_list_entries_expanded, workspace_list_rect,
+        workspace_list_scroll_metrics, workspace_list_scrollbar_rect, workspace_parent_group_state,
+        AgentPanelEntry, WorkspaceListEntry,
     },
 };
 
@@ -245,7 +248,7 @@ fn compute_view_internal(
 
     if !app.sidebar_collapsed {
         app.workspace_scroll = normalized_workspace_scroll(app, sidebar_area, app.workspace_scroll);
-        let (_, detail_area) = expanded_sidebar_sections(sidebar_area, app.sidebar_section_split);
+        let (_, detail_area) = expanded_sidebar_sections(app, sidebar_area);
         let max_agent_scroll = agent_panel_scroll_metrics(app, detail_area).max_offset_from_bottom;
         app.agent_panel_scroll = app.agent_panel_scroll.min(max_agent_scroll);
     } else {
@@ -289,6 +292,7 @@ fn compute_view_internal(
     if resize_panes {
         resize_background_tab_panes_for_desktop(app, terminal_runtimes, main_area, cell_size);
         resize_popup_pane(app, terminal_runtimes, terminal_area, cell_size);
+        resize_sidebar_pty(app, terminal_runtimes, sidebar_area, cell_size);
     }
 
     let toast_hit_area = app
diff --git a/src/ui/keybind_help.rs b/src/ui/keybind_help.rs
index 1f01c60c..1effdccc 100644
--- a/src/ui/keybind_help.rs
+++ b/src/ui/keybind_help.rs
@@ -152,6 +152,10 @@ pub(super) fn keybind_help_groups(app: &AppState) -> Vec<HelpGroup> {
         help_entry(keybind_label(&kb.resize_pane_up), "resize pane up"),
         help_entry(keybind_label(&kb.resize_pane_right), "resize pane right"),
         help_entry(keybind_label(&kb.toggle_sidebar), "toggle sidebar"),
+        help_entry(
+            keybind_label(&kb.toggle_sidebar_fill),
+            "toggle sidebar fill",
+        ),
         help_entry(keybind_label(&kb.focus_pane_left), "focus pane left"),
         help_entry(keybind_label(&kb.focus_pane_down), "focus pane down"),
         help_entry(keybind_label(&kb.focus_pane_up), "focus pane up"),
diff --git a/src/ui/sidebar.rs b/src/ui/sidebar.rs
index 9eede99c..637a2c94 100644
--- a/src/ui/sidebar.rs
+++ b/src/ui/sidebar.rs
@@ -19,6 +19,12 @@ use crate::terminal::TerminalRuntimeRegistry;
 
 const WORKSPACE_SECTION_HEADER_ROWS: u16 = 2;
 const AGENT_PANEL_HEADER_ROWS: u16 = 3;
+const SIDEBAR_PTY_MIN_ROWS: u16 = 3;
+const SIDEBAR_PTY_MAX_ROWS: u16 = 16;
+const SIDEBAR_PTY_DIVIDER_ROWS: u16 = 1;
+/// Rows the spaces/agents sections need below the pty strip before the strip
+/// is dropped, matching the minimum height the split math handles gracefully.
+const SIDEBAR_PTY_MIN_REMAINING_ROWS: u16 = 6;
 
 pub(crate) struct AgentPanelEntry {
     pub ws_idx: usize,
@@ -56,25 +62,101 @@ fn sidebar_section_heights(total_h: u16, split_ratio: f32) -> (u16, u16) {
     (ws_h, detail_h)
 }
 
-pub(crate) fn expanded_sidebar_sections(area: Rect, split_ratio: f32) -> (Rect, Rect) {
+/// Sidebar pty grid width for an expanded sidebar of `sidebar_width` columns:
+/// the full content width next to the 1-column separator.
+pub(crate) fn sidebar_pty_grid_cols(sidebar_width: u16) -> u16 {
+    sidebar_width.saturating_sub(1).max(1)
+}
+
+/// Sidebar pty grid height for a grid `cols` wide. Without an override the
+/// height approximates a 4:3 visual aspect ratio: terminal cells are roughly
+/// twice as tall as wide, so rows ≈ cols * 3/4 * 1/2, rounded to nearest.
+pub(crate) fn sidebar_pty_rows_for_width(cols: u16, rows_override: Option<u16>) -> u16 {
+    let rows = rows_override.unwrap_or_else(|| ((u32::from(cols) * 3 + 4) / 8) as u16);
+    rows.clamp(SIDEBAR_PTY_MIN_ROWS, SIDEBAR_PTY_MAX_ROWS)
+}
+
+/// Grid size (cols, rows) of the sidebar pty section, or None when the
+/// section is hidden: no pty running, collapsed sidebar, or too little room
+/// left for the spaces/agents sections. Recomputed from the current sidebar
+/// width every frame so width and split changes propagate to the pty.
+pub(crate) fn sidebar_pty_grid_size(app: &AppState, area: Rect) -> Option<(u16, u16)> {
+    let pty = app.sidebar_pty.as_ref()?;
+    if app.sidebar_collapsed {
+        return None;
+    }
+    let content_width = area.width.saturating_sub(1);
+    if content_width == 0 || area.height == 0 {
+        return None;
+    }
+    if sidebar_pty_fill_active(app) {
+        // Fill mode: the live pty owns the entire expanded-sidebar rectangle
+        // (minus the separator column). No aspect clamp and no reserved room
+        // for navigation, which is not rendered while fill is active.
+        return Some((content_width, area.height));
+    }
+    let rows = sidebar_pty_rows_for_width(content_width, pty.rows_override);
+    (area.height >= rows + SIDEBAR_PTY_DIVIDER_ROWS + SIDEBAR_PTY_MIN_REMAINING_ROWS)
+        .then_some((content_width, rows))
+}
+
+/// Whether the sidebar pty is currently rendered as the full-rectangle fill
+/// surface. True only when fill mode is requested (config default or runtime
+/// toggle) AND a sidebar pty is live AND the sidebar is expanded. When the pty
+/// is absent, failed to spawn, or has died, this is false so the sidebar
+/// visibly falls back to ordinary workspace/agent navigation.
+pub(crate) fn sidebar_pty_fill_active(app: &AppState) -> bool {
+    app.sidebar_pty_fill && app.sidebar_pty.is_some() && !app.sidebar_collapsed
+}
+
+/// Total rows the sidebar pty section occupies at the top of the expanded
+/// sidebar (grid plus divider), or 0 when hidden. In fill mode the section is
+/// the full height and carries no divider.
+pub(crate) fn sidebar_pty_section_height(app: &AppState, area: Rect) -> u16 {
+    if sidebar_pty_fill_active(app) {
+        return sidebar_pty_grid_size(app, area).map_or(0, |(_, rows)| rows);
+    }
+    sidebar_pty_grid_size(app, area).map_or(0, |(_, rows)| rows + SIDEBAR_PTY_DIVIDER_ROWS)
+}
+
+pub(crate) fn sidebar_pty_grid_rect(app: &AppState, area: Rect) -> Option<Rect> {
+    let (cols, rows) = sidebar_pty_grid_size(app, area)?;
+    Some(Rect::new(area.x, area.y, cols, rows))
+}
+
+/// Portion of the expanded sidebar left for the spaces/agents sections after
+/// the pty strip is carved off the top.
+fn sidebar_sections_area(app: &AppState, area: Rect) -> Rect {
+    let pty_h = sidebar_pty_section_height(app, area);
+    Rect::new(
+        area.x,
+        area.y.saturating_add(pty_h),
+        area.width,
+        area.height.saturating_sub(pty_h),
+    )
+}
+
+pub(crate) fn expanded_sidebar_sections(app: &AppState, area: Rect) -> (Rect, Rect) {
+    let area = sidebar_sections_area(app, area);
     let content = Rect::new(area.x, area.y, area.width.saturating_sub(1), area.height);
     if content.width == 0 || content.height == 0 {
         return (Rect::default(), Rect::default());
     }
 
-    let (ws_h, detail_h) = sidebar_section_heights(content.height, split_ratio);
+    let (ws_h, detail_h) = sidebar_section_heights(content.height, app.sidebar_section_split);
     let ws_area = Rect::new(content.x, content.y, content.width, ws_h);
     let detail_area = Rect::new(content.x, content.y + ws_h, content.width, detail_h);
     (ws_area, detail_area)
 }
 
-pub(crate) fn sidebar_section_divider_rect(area: Rect, split_ratio: f32) -> Rect {
+pub(crate) fn sidebar_section_divider_rect(app: &AppState, area: Rect) -> Rect {
+    let area = sidebar_sections_area(app, area);
     let content = Rect::new(area.x, area.y, area.width.saturating_sub(1), area.height);
     if content.width == 0 || content.height < 6 {
         return Rect::default();
     }
 
-    let (ws_h, _) = sidebar_section_heights(content.height, split_ratio);
+    let (ws_h, _) = sidebar_section_heights(content.height, app.sidebar_section_split);
     Rect::new(content.x, content.y + ws_h, content.width, 1)
 }
 
@@ -311,7 +393,7 @@ pub(crate) fn next_entry_is_indented_workspace(entries: &[WorkspaceListEntry], i
 }
 
 pub(crate) fn normalized_workspace_scroll(app: &AppState, area: Rect, requested: usize) -> usize {
-    let ws_area = workspace_list_rect(area, app.sidebar_section_split);
+    let ws_area = workspace_list_rect(app, area);
     let body = workspace_list_body_rect(ws_area, false);
     if body.height == 0 {
         return requested;
@@ -435,8 +517,8 @@ fn workspace_list_entries_inner(app: &AppState, force_expanded: bool) -> Vec<Wor
     entries
 }
 
-pub(crate) fn workspace_list_rect(area: Rect, split_ratio: f32) -> Rect {
-    let (ws_area, _) = expanded_sidebar_sections(area, split_ratio);
+pub(crate) fn workspace_list_rect(app: &AppState, area: Rect) -> Rect {
+    let (ws_area, _) = expanded_sidebar_sections(app, area);
     ws_area
 }
 
@@ -659,7 +741,7 @@ pub(crate) fn compute_workspace_list_areas(
     app: &AppState,
     area: Rect,
 ) -> (Vec<crate::app::state::WorkspaceCardArea>, Vec<()>) {
-    let ws_area = workspace_list_rect(area, app.sidebar_section_split);
+    let ws_area = workspace_list_rect(app, area);
     if ws_area == Rect::default() {
         return (Vec::new(), Vec::new());
     }
@@ -993,13 +1075,76 @@ pub(super) fn render_sidebar(
         buf[(sep_x, y)].set_style(sep_style);
     }
 
-    let (ws_area, detail_area) = expanded_sidebar_sections(area, app.sidebar_section_split);
+    render_sidebar_pty(app, terminal_runtimes, frame, area);
+
+    // Fill mode: the sidebar pty owns the whole rectangle; workspace/agent
+    // navigation is not rendered while it is active. The underlying nav model,
+    // keyboard, CLI/API, and reorder code are all untouched — this is a
+    // render-slot substitution only. The toggle affordance still renders so
+    // the collapse control stays reachable.
+    if !sidebar_pty_fill_active(app) {
+        let (ws_area, detail_area) = expanded_sidebar_sections(app, area);
 
-    render_workspace_list(app, terminal_runtimes, frame, ws_area, is_navigating);
-    render_agent_detail(app, terminal_runtimes, frame, detail_area);
+        render_workspace_list(app, terminal_runtimes, frame, ws_area, is_navigating);
+        render_agent_detail(app, terminal_runtimes, frame, detail_area);
+    }
     render_sidebar_toggle(app, frame, area, false, p);
 }
 
+/// Embedded sidebar terminal pinned above the spaces section. The pane has no
+/// keyboard focus, so the host cursor is never revealed inside it.
+fn render_sidebar_pty(
+    app: &AppState,
+    terminal_runtimes: &TerminalRuntimeRegistry,
+    frame: &mut Frame,
+    area: Rect,
+) {
+    let Some(pty) = app.sidebar_pty.as_ref() else {
+        return;
+    };
+    let Some(grid) = sidebar_pty_grid_rect(app, area) else {
+        return;
+    };
+    if let Some(rt) = terminal_runtimes.get(&pty.terminal_id) {
+        rt.render(frame, grid, false);
+    }
+
+    // Fill mode occupies the full rectangle, so there is no navigation below
+    // it and no divider to draw. Skipping this also keeps the divider row
+    // inside the sidebar bounds.
+    if sidebar_pty_fill_active(app) {
+        return;
+    }
+
+    let divider_y = grid.y + grid.height;
+    let divider_style = Style::default().fg(app.palette.surface_dim);
+    let buf = frame.buffer_mut();
+    for x in grid.x..grid.x + grid.width {
+        buf[(x, divider_y)].set_symbol("─");
+        buf[(x, divider_y)].set_style(divider_style);
+    }
+}
+
+/// Keep the sidebar pty's winsize in step with the geometry derived from the
+/// current sidebar width. Called from the per-frame resize hook; a no-op when
+/// the section is hidden so a collapsed sidebar keeps the last size.
+pub(super) fn resize_sidebar_pty(
+    app: &AppState,
+    terminal_runtimes: &TerminalRuntimeRegistry,
+    area: Rect,
+    cell_size: crate::kitty_graphics::HostCellSize,
+) {
+    let Some(pty) = app.sidebar_pty.as_ref() else {
+        return;
+    };
+    let Some((cols, rows)) = sidebar_pty_grid_size(app, area) else {
+        return;
+    };
+    if let Some(rt) = terminal_runtimes.get(&pty.terminal_id) {
+        rt.resize(rows, cols, cell_size.width_px, cell_size.height_px);
+    }
+}
+
 fn resolved_token_spans(
     resolved: &[ResolvedToken],
     state_icon: (&str, Style),
@@ -1662,7 +1807,7 @@ mod tests {
             .draw(|frame| render_sidebar(&app, &TerminalRuntimeRegistry::new(), frame, area))
             .unwrap();
         let buffer = terminal.backend().buffer();
-        let (_, agent_area) = expanded_sidebar_sections(area, app.sidebar_section_split);
+        let (_, agent_area) = expanded_sidebar_sections(&app, area);
         let body = agent_panel_body_rect(agent_area, false);
 
         let first = row_text(buffer, body.y, 25);
@@ -1713,7 +1858,7 @@ rows = [[{ token = "workspace", bold = false }, { token = "agent", dim = false }
         terminal
             .draw(|frame| render_sidebar(&app, &TerminalRuntimeRegistry::new(), frame, area))
             .unwrap();
-        let (_, agent_area) = expanded_sidebar_sections(area, app.sidebar_section_split);
+        let (_, agent_area) = expanded_sidebar_sections(&app, area);
         let body = agent_panel_body_rect(agent_area, false);
         let buffer = terminal.backend().buffer();
         let workspace = buffer[(find_symbol_x(buffer, body.y, body.width, "o"), body.y)].style();
@@ -1914,7 +2059,7 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
             .draw(|frame| render_sidebar(&app, &TerminalRuntimeRegistry::new(), frame, area))
             .unwrap();
         let buffer = terminal.backend().buffer();
-        let (_, agent_area) = expanded_sidebar_sections(area, app.sidebar_section_split);
+        let (_, agent_area) = expanded_sidebar_sections(&app, area);
         let body = agent_panel_body_rect(agent_area, false);
         let first = row_text(buffer, body.y, 17);
 
@@ -1944,7 +2089,7 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
         renderer
             .draw(|frame| render_sidebar(&app, &TerminalRuntimeRegistry::new(), frame, area))
             .unwrap();
-        let (_, agent_area) = expanded_sidebar_sections(area, app.sidebar_section_split);
+        let (_, agent_area) = expanded_sidebar_sections(&app, area);
         let body = agent_panel_body_rect(agent_area, false);
         let rendered = row_text(renderer.backend().buffer(), body.y, 9);
 
@@ -2020,7 +2165,7 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
         app.workspaces = vec![Workspace::test_new("one"), Workspace::test_new("two")];
         app.sidebar_spaces.rows = vec![vec![crate::config::SpaceSidebarToken::Workspace]; 6];
         let area = Rect::new(0, 0, 20, 10);
-        let workspace_area = workspace_list_rect(area, app.sidebar_section_split);
+        let workspace_area = workspace_list_rect(&app, area);
         let body = workspace_list_body_rect(workspace_area, false);
 
         let metrics = workspace_list_scroll_metrics(&app, workspace_area);
@@ -2533,7 +2678,10 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
 
     #[test]
     fn expanded_sidebar_sections_handle_tiny_heights() {
-        let (ws_area, detail_area) = expanded_sidebar_sections(Rect::new(0, 0, 20, 5), 0.9);
+        let mut app = crate::app::state::AppState::test_new();
+        app.sidebar_section_split = 0.9;
+
+        let (ws_area, detail_area) = expanded_sidebar_sections(&app, Rect::new(0, 0, 20, 5));
 
         assert_eq!(ws_area, Rect::new(0, 0, 19, 3));
         assert_eq!(detail_area, Rect::new(0, 3, 19, 2));
@@ -2541,11 +2689,217 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
 
     #[test]
     fn sidebar_section_divider_is_hidden_for_tiny_heights() {
-        let divider = sidebar_section_divider_rect(Rect::new(0, 0, 20, 5), 0.5);
+        let mut app = crate::app::state::AppState::test_new();
+        app.sidebar_section_split = 0.5;
+
+        let divider = sidebar_section_divider_rect(&app, Rect::new(0, 0, 20, 5));
 
         assert_eq!(divider, Rect::default());
     }
 
+    fn app_with_sidebar_pty(rows_override: Option<u16>) -> crate::app::state::AppState {
+        let mut app = crate::app::state::AppState::test_new();
+        let terminal_id = crate::terminal::TerminalId::alloc();
+        app.terminals.insert(
+            terminal_id.clone(),
+            crate::terminal::TerminalState::new(
+                terminal_id.clone(),
+                std::path::PathBuf::from("/sidebar-pty"),
+            ),
+        );
+        app.sidebar_pty = Some(crate::app::state::SidebarPtyState {
+            pane_id: crate::layout::PaneId::alloc(),
+            terminal_id,
+            rows_override,
+        });
+        app
+    }
+
+    #[test]
+    fn sidebar_pty_rows_approximate_a_4_3_visual_aspect_ratio() {
+        // Cells are ~twice as tall as wide, so visual 4:3 is rows ≈ cols * 3/8.
+        assert_eq!(sidebar_pty_rows_for_width(31, None), 12);
+        assert_eq!(sidebar_pty_rows_for_width(24, None), 9);
+        // Clamped so narrow sidebars keep a usable grid and wide sidebars
+        // cannot eat the whole panel.
+        assert_eq!(sidebar_pty_rows_for_width(4, None), SIDEBAR_PTY_MIN_ROWS);
+        assert_eq!(sidebar_pty_rows_for_width(200, None), SIDEBAR_PTY_MAX_ROWS);
+        // Overrides win but stay clamped.
+        assert_eq!(sidebar_pty_rows_for_width(31, Some(5)), 5);
+        assert_eq!(
+            sidebar_pty_rows_for_width(31, Some(40)),
+            SIDEBAR_PTY_MAX_ROWS
+        );
+    }
+
+    #[test]
+    fn sidebar_pty_section_is_absent_without_a_running_pty() {
+        let mut app = crate::app::state::AppState::test_new();
+        app.sidebar_section_split = 0.5;
+        let area = Rect::new(0, 0, 32, 40);
+
+        assert_eq!(sidebar_pty_section_height(&app, area), 0);
+        assert_eq!(sidebar_pty_grid_rect(&app, area), None);
+        let (ws_area, detail_area) = expanded_sidebar_sections(&app, area);
+        assert_eq!(ws_area, Rect::new(0, 0, 31, 20));
+        assert_eq!(detail_area, Rect::new(0, 20, 31, 20));
+    }
+
+    #[test]
+    fn sidebar_pty_section_spans_full_width_and_shifts_sections_down() {
+        let mut app = app_with_sidebar_pty(None);
+        app.sidebar_section_split = 0.5;
+        let area = Rect::new(0, 0, 32, 40);
+
+        // 31 content cols → 12 grid rows + 1 divider row.
+        assert_eq!(
+            sidebar_pty_grid_rect(&app, area),
+            Some(Rect::new(0, 0, 31, 12))
+        );
+        assert_eq!(sidebar_pty_section_height(&app, area), 13);
+
+        let (ws_area, detail_area) = expanded_sidebar_sections(&app, area);
+        assert_eq!(ws_area, Rect::new(0, 13, 31, 14));
+        assert_eq!(detail_area, Rect::new(0, 27, 31, 13));
+        assert_eq!(
+            sidebar_section_divider_rect(&app, area),
+            Rect::new(0, 27, 31, 1)
+        );
+    }
+
+    #[test]
+    fn sidebar_pty_rows_follow_the_current_sidebar_width() {
+        let app = app_with_sidebar_pty(None);
+
+        assert_eq!(
+            sidebar_pty_grid_size(&app, Rect::new(0, 0, 32, 40)),
+            Some((31, 12))
+        );
+        assert_eq!(
+            sidebar_pty_grid_size(&app, Rect::new(0, 0, 25, 40)),
+            Some((24, 9))
+        );
+    }
+
+    #[test]
+    fn sidebar_pty_rows_override_takes_precedence_over_derived_height() {
+        let app = app_with_sidebar_pty(Some(4));
+
+        assert_eq!(
+            sidebar_pty_grid_size(&app, Rect::new(0, 0, 32, 40)),
+            Some((31, 4))
+        );
+    }
+
+    #[test]
+    fn sidebar_pty_section_is_dropped_for_tiny_heights() {
+        let mut app = app_with_sidebar_pty(None);
+        app.sidebar_section_split = 0.9;
+        // 31 content cols derive 12 grid rows; 12 + 1 + 6 = 19 rows required.
+        let area = Rect::new(0, 0, 32, 18);
+
+        assert_eq!(sidebar_pty_section_height(&app, area), 0);
+        assert_eq!(sidebar_pty_grid_rect(&app, area), None);
+
+        let tiny = Rect::new(0, 0, 20, 5);
+        assert_eq!(sidebar_pty_grid_rect(&app, tiny), None);
+        let (ws_area, detail_area) = expanded_sidebar_sections(&app, tiny);
+        assert_eq!(ws_area, Rect::new(0, 0, 19, 3));
+        assert_eq!(detail_area, Rect::new(0, 3, 19, 2));
+    }
+
+    #[test]
+    fn sidebar_pty_section_is_hidden_when_sidebar_is_collapsed() {
+        let mut app = app_with_sidebar_pty(None);
+        app.sidebar_collapsed = true;
+
+        assert_eq!(sidebar_pty_grid_rect(&app, Rect::new(0, 0, 32, 40)), None);
+        assert_eq!(sidebar_pty_section_height(&app, Rect::new(0, 0, 32, 40)), 0);
+    }
+
+    #[test]
+    fn fill_mode_gives_the_pty_the_full_rectangle_and_removes_nav_sections() {
+        let mut app = app_with_sidebar_pty(None);
+        app.sidebar_pty_fill = true;
+        app.sidebar_section_split = 0.5;
+        let area = Rect::new(0, 0, 32, 40);
+
+        assert!(sidebar_pty_fill_active(&app));
+        // The grid is the full expanded-sidebar rectangle minus the separator
+        // column; no aspect clamp and no divider row.
+        assert_eq!(
+            sidebar_pty_grid_rect(&app, area),
+            Some(Rect::new(0, 0, 31, 40))
+        );
+        assert_eq!(sidebar_pty_section_height(&app, area), 40);
+        // Navigation sections collapse to nothing while fill is active.
+        let (ws_area, detail_area) = expanded_sidebar_sections(&app, area);
+        assert_eq!(ws_area, Rect::default());
+        assert_eq!(detail_area, Rect::default());
+        assert_eq!(sidebar_section_divider_rect(&app, area), Rect::default());
+    }
+
+    #[test]
+    fn fill_mode_is_inactive_without_a_live_pty() {
+        // Fill requested but no pty running: the sidebar falls back to normal
+        // navigation geometry, exactly as if fill were off.
+        let mut app = crate::app::state::AppState::test_new();
+        app.sidebar_pty_fill = true;
+        app.sidebar_section_split = 0.5;
+        let area = Rect::new(0, 0, 32, 40);
+
+        assert!(!sidebar_pty_fill_active(&app));
+        assert_eq!(sidebar_pty_grid_rect(&app, area), None);
+        assert_eq!(sidebar_pty_section_height(&app, area), 0);
+        let (ws_area, detail_area) = expanded_sidebar_sections(&app, area);
+        assert_eq!(ws_area, Rect::new(0, 0, 31, 20));
+        assert_eq!(detail_area, Rect::new(0, 20, 31, 20));
+    }
+
+    #[test]
+    fn fill_mode_is_inactive_when_sidebar_is_collapsed() {
+        let mut app = app_with_sidebar_pty(None);
+        app.sidebar_pty_fill = true;
+        app.sidebar_collapsed = true;
+
+        assert!(!sidebar_pty_fill_active(&app));
+        assert_eq!(sidebar_pty_grid_rect(&app, Rect::new(0, 0, 32, 40)), None);
+    }
+
+    #[test]
+    fn toggling_fill_switches_between_full_rect_and_capped_layouts() {
+        let mut app = app_with_sidebar_pty(None);
+        app.sidebar_section_split = 0.5;
+        let area = Rect::new(0, 0, 32, 40);
+
+        // Off (default): capped 12-row pty above navigation sections.
+        assert!(!sidebar_pty_fill_active(&app));
+        assert_eq!(
+            sidebar_pty_grid_rect(&app, area),
+            Some(Rect::new(0, 0, 31, 12))
+        );
+        let (ws_off, _) = expanded_sidebar_sections(&app, area);
+        assert_eq!(ws_off, Rect::new(0, 13, 31, 14));
+
+        // Toggle on → full rectangle, no nav.
+        app.toggle_sidebar_pty_fill();
+        assert!(sidebar_pty_fill_active(&app));
+        assert_eq!(
+            sidebar_pty_grid_rect(&app, area),
+            Some(Rect::new(0, 0, 31, 40))
+        );
+        assert_eq!(expanded_sidebar_sections(&app, area).0, Rect::default());
+
+        // Toggle off → back to the capped layout, no restart/config rewrite.
+        app.toggle_sidebar_pty_fill();
+        assert!(!sidebar_pty_fill_active(&app));
+        assert_eq!(
+            sidebar_pty_grid_rect(&app, area),
+            Some(Rect::new(0, 0, 31, 12))
+        );
+        assert_eq!(expanded_sidebar_sections(&app, area).0, ws_off);
+    }
+
     #[test]
     fn grouped_child_label_keeps_custom_workspace_name() {
         assert_eq!(
@@ -2633,7 +2987,7 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
         app.sidebar_spaces.row_gap = 0;
         let area = Rect::new(0, 0, 30, 20);
         app.view.workspace_card_areas = compute_workspace_card_areas(&app, area);
-        let list_area = workspace_list_rect(area, app.sidebar_section_split);
+        let list_area = workspace_list_rect(&app, area);
 
         let mut terminal = Terminal::new(TestBackend::new(area.width, area.height)).unwrap();
         terminal
@@ -2674,7 +3028,7 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
         let area = Rect::new(0, 0, 30, 10);
         app.view.workspace_card_areas = compute_workspace_card_areas(&app, area);
         assert_eq!(app.view.workspace_card_areas.len(), 2);
-        let list_area = workspace_list_rect(area, app.sidebar_section_split);
+        let list_area = workspace_list_rect(&app, area);
 
         let mut terminal = Terminal::new(TestBackend::new(area.width, area.height)).unwrap();
         terminal
@@ -2766,7 +3120,7 @@ rows = [[{ token = "git_status", fg = "#123456" }]]
         app.sidebar_spaces.row_gap = 0;
         let area = Rect::new(0, 0, 30, 20);
         app.view.workspace_card_areas = compute_workspace_card_areas(&app, area);
-        let list_area = workspace_list_rect(area, app.sidebar_section_split);
+        let list_area = workspace_list_rect(&app, area);
         let indicator_row = workspace_drop_indicator_row(
             &app,
             &app.view.workspace_card_areas,
