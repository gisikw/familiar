{
  "name": "familiar-server",
  "version": "0.1.0",
  "private": true,
  "description": "Familiar server — standalone web presence: SSE firehose hub, ingress/egress relay, and a browser terminal that attaches to the running herdr session.",
  "type": "module",
  "scripts": {
    "start": "node --experimental-transform-types src/main.ts",
    "postinstall": "node scripts/vendor-assets.cjs"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "restty": "^0.2.6",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/ws": "^8.18.1"
  }
}
